-- V56: Tao BIZ_HA_TANG_MANG_HIS (Group P snapshot theo ky bao cao).
-- Bang live giu nguyen — khong them cot. Snapshot duoc tao boi FinalizeKyBaoCaoService.

BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_HA_TANG_MANG_HIS (
      ID                    NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID             NUMBER(19)      NOT NULL,
      DON_VI_ID             NUMBER(19)      NOT NULL,
      LOAI_DV_THONG_KE      NVARCHAR2(30)   NOT NULL,
      SO_DON_VI_TRUC_THUOC  NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_DA_KET_NOI_BCANET  NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_DUONG_TRUYEN_VNPT  NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_DUONG_TRUYEN_KHAC  NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_KET_NOI_INTERNET   NUMBER(10)      DEFAULT 0 NOT NULL,
      GHI_CHU               NVARCHAR2(2000),
      KY_BAO_CAO_CODE       NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID     NUMBER(19),
      SNAPSHOT_CREATED_AT   TIMESTAMP       NOT NULL,
      CREATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY            NUMBER(19),
      UPDATED_BY            NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_HTM_HIS_DONVI_KY ON BIZ_HA_TANG_MANG_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_HTM_HIS_BATCH ON BIZ_HA_TANG_MANG_HIS (SNAPSHOT_BATCH_ID)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
