-- V54: Them VALID_FROM, VERSION_NO vao BIZ_THIET_BI_CNTT (SCD2).
-- Tao BIZ_THIET_BI_CNTT_HIS va BIZ_THIET_BI_UNG_DUNG_HIS.

-- Them VALID_FROM vao live table
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE BIZ_THIET_BI_CNTT ADD VALID_FROM TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL';
EXCEPTION WHEN OTHERS THEN IF SQLCODE NOT IN (-1430, -1442) THEN RAISE; END IF;
END;
/

-- Them VERSION_NO vao live table
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE BIZ_THIET_BI_CNTT ADD VERSION_NO NUMBER(10) DEFAULT 1 NOT NULL';
EXCEPTION WHEN OTHERS THEN IF SQLCODE NOT IN (-1430, -1442) THEN RAISE; END IF;
END;
/

-- Tao BIZ_THIET_BI_CNTT_HIS
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_THIET_BI_CNTT_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      LOAI_THIET_BI_ID    NUMBER(19)      NOT NULL,
      TEN_THIET_BI        NVARCHAR2(200),
      HANG_SAN_XUAT       NVARCHAR2(100),
      MODEL               NVARCHAR2(100),
      CAU_HINH            NVARCHAR2(500),
      HE_DIEU_HANH        NVARCHAR2(100),
      DON_VI_SU_DUNG      NVARCHAR2(200),
      SO_LUONG_TONG       NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_LUONG_HIEN_DUNG  NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_LUONG_HONG       NUMBER(10)      DEFAULT 0 NOT NULL,
      TINH_TRANG          NVARCHAR2(100),
      GHI_CHU             NVARCHAR2(2000),
      VALID_FROM          TIMESTAMP       NOT NULL,
      VALID_TO            TIMESTAMP       NOT NULL,
      VERSION_NO          NUMBER(10)      NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_TBC_HIS_DONVI_VALID ON BIZ_THIET_BI_CNTT_HIS (DON_VI_ID, VALID_FROM, VALID_TO)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_TBC_HIS_SRC ON BIZ_THIET_BI_CNTT_HIS (SOURCE_ID)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

-- Tao BIZ_THIET_BI_UNG_DUNG_HIS (snapshot ung dung khi thiet bi tao version)
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_THIET_BI_UNG_DUNG_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_THIET_BI_ID  NUMBER(19)      NOT NULL,
      SOURCE_HE_THONG_ID  NUMBER(19)      NOT NULL,
      VALID_FROM          TIMESTAMP       NOT NULL,
      VALID_TO            TIMESTAMP       NOT NULL,
      VERSION_NO          NUMBER(10)      NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_TBUD_HIS_THIETBI_VALID ON BIZ_THIET_BI_UNG_DUNG_HIS (SOURCE_THIET_BI_ID, VALID_FROM)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
