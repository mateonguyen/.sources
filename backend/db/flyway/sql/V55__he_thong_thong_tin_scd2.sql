-- V55: Them VALID_FROM, VERSION_NO vao BIZ_HE_THONG_THONG_TIN va BIZ_HTTT_TIEU_CHUAN (SCD2 Group S).
-- Tao BIZ_HE_THONG_THONG_TIN_HIS va BIZ_HTTT_TIEU_CHUAN_HIS.

-- Them VALID_FROM vao BIZ_HE_THONG_THONG_TIN
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE BIZ_HE_THONG_THONG_TIN ADD VALID_FROM TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL';
EXCEPTION WHEN OTHERS THEN IF SQLCODE NOT IN (-1430, -1442) THEN RAISE; END IF;
END;
/

-- Them VERSION_NO vao BIZ_HE_THONG_THONG_TIN
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE BIZ_HE_THONG_THONG_TIN ADD VERSION_NO NUMBER(10) DEFAULT 1 NOT NULL';
EXCEPTION WHEN OTHERS THEN IF SQLCODE NOT IN (-1430, -1442) THEN RAISE; END IF;
END;
/

-- Tao BIZ_HE_THONG_THONG_TIN_HIS
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_HE_THONG_THONG_TIN_HIS (
      ID                    NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID             NUMBER(19)      NOT NULL,
      DON_VI_ID             NUMBER(19)      NOT NULL,
      LOAI_PHAN_MEM         NVARCHAR2(20)   NOT NULL,
      TEN_PHAN_MEM          NVARCHAR2(200)  NOT NULL,
      MA_PHAN_MEM           NVARCHAR2(100),
      DON_VI_PHAT_TRIEN     NVARCHAR2(200),
      DON_VI_QUAN_LY        NVARCHAR2(300),
      NAM_TRIEN_KHAI        NUMBER(4),
      PHAM_VI_HOAT_DONG     NVARCHAR2(500),
      UNG_DUNG_CN_MOI       NUMBER(1)       DEFAULT 0 NOT NULL,
      KHA_NANG_TICH_HOP     NUMBER(1)       DEFAULT 0 NOT NULL,
      DA_CONG_NHAN_SANG_KIEN NUMBER(1)      DEFAULT 0 NOT NULL,
      GHI_CHU               NVARCHAR2(2000),
      VALID_FROM            TIMESTAMP       NOT NULL,
      VALID_TO              TIMESTAMP       NOT NULL,
      VERSION_NO            NUMBER(10)      NOT NULL,
      CREATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY            NUMBER(19),
      UPDATED_BY            NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_HTTT_HIS_DONVI_VALID ON BIZ_HE_THONG_THONG_TIN_HIS (DON_VI_ID, VALID_FROM, VALID_TO)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_HTTT_HIS_SRC ON BIZ_HE_THONG_THONG_TIN_HIS (SOURCE_ID)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

-- Them VALID_FROM vao BIZ_HTTT_TIEU_CHUAN
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE BIZ_HTTT_TIEU_CHUAN ADD VALID_FROM TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL';
EXCEPTION WHEN OTHERS THEN IF SQLCODE NOT IN (-1430, -1442) THEN RAISE; END IF;
END;
/

-- Them VERSION_NO vao BIZ_HTTT_TIEU_CHUAN
BEGIN
  EXECUTE IMMEDIATE 'ALTER TABLE BIZ_HTTT_TIEU_CHUAN ADD VERSION_NO NUMBER(10) DEFAULT 1 NOT NULL';
EXCEPTION WHEN OTHERS THEN IF SQLCODE NOT IN (-1430, -1442) THEN RAISE; END IF;
END;
/

-- Tao BIZ_HTTT_TIEU_CHUAN_HIS
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_HTTT_TIEU_CHUAN_HIS (
      ID                    NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID             NUMBER(19)      NOT NULL,
      DON_VI_ID             NUMBER(19)      NOT NULL,
      MA_HE_THONG           NVARCHAR2(50)   NOT NULL,
      DVT                   NVARCHAR2(20),
      SO_H05                NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_TINH               NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_XA                 NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_DV_TRUC_THUOC_BO   NUMBER(10)      DEFAULT 0 NOT NULL,
      GHI_CHU               NVARCHAR2(2000),
      VALID_FROM            TIMESTAMP       NOT NULL,
      VALID_TO              TIMESTAMP       NOT NULL,
      VERSION_NO            NUMBER(10)      NOT NULL,
      CREATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY            NUMBER(19),
      UPDATED_BY            NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_HTTTTC_HIS_DONVI_VALID ON BIZ_HTTT_TIEU_CHUAN_HIS (DON_VI_ID, VALID_FROM, VALID_TO)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_HTTTTC_HIS_SRC ON BIZ_HTTT_TIEU_CHUAN_HIS (SOURCE_ID)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
