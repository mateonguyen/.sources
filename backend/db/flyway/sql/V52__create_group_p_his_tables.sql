-- V52: Tao bang _HIS cho 11 bang Group P (Snapshot-in-Place).
-- Du lieu IS_LATEST=0 duoc migrate sang _HIS.

-- ============================================================
-- BIZ_DAO_TAO_BOI_DUONG_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_DAO_TAO_BOI_DUONG_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      TEN_KHOA_HOC        NVARCHAR2(300)  NOT NULL,
      DON_VI_TO_CHUC      NVARCHAR2(200)  NOT NULL,
      HINH_THUC           NVARCHAR2(100),
      SO_LUONG_HV         NUMBER(10),
      THOI_GIAN_TU        DATE,
      THOI_GIAN_DEN       DATE,
      GHI_CHU             NVARCHAR2(2000),
      KY_BAO_CAO_CODE     NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID   NUMBER(19),
      SNAPSHOT_CREATED_AT TIMESTAMP       NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_DTBD_HIS_DONVI_KY ON BIZ_DAO_TAO_BOI_DUONG_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_DTBD_HIS_BATCH ON BIZ_DAO_TAO_BOI_DUONG_HIS (SNAPSHOT_BATCH_ID)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_DAO_TAO_BOI_DUONG_HIS (
  SOURCE_ID, DON_VI_ID, TEN_KHOA_HOC, DON_VI_TO_CHUC, HINH_THUC,
  SO_LUONG_HV, THOI_GIAN_TU, THOI_GIAN_DEN, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, TEN_KHOA_HOC, DON_VI_TO_CHUC, HINH_THUC,
  SO_LUONG_HV, THOI_GIAN_TU, THOI_GIAN_DEN, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_DAO_TAO_BOI_DUONG
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_DAO_TAO_HOC_VIEN_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_DAO_TAO_HOC_VIEN_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      NAM                 NUMBER(4)       NOT NULL,
      NOI_DUNG_DAO_TAO    NVARCHAR2(50)   NOT NULL,
      SO_TIEN_SI          NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_THAC_SI          NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_DAI_HOC          NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_CAO_DANG         NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_TRUNG_CAP        NUMBER(10)      DEFAULT 0 NOT NULL,
      GHI_CHU             NVARCHAR2(2000),
      KY_BAO_CAO_CODE     NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID   NUMBER(19),
      SNAPSHOT_CREATED_AT TIMESTAMP       NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_DTHV_HIS_DONVI_KY ON BIZ_DAO_TAO_HOC_VIEN_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_DAO_TAO_HOC_VIEN_HIS (
  SOURCE_ID, DON_VI_ID, NAM, NOI_DUNG_DAO_TAO,
  SO_TIEN_SI, SO_THAC_SI, SO_DAI_HOC, SO_CAO_DANG, SO_TRUNG_CAP, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, NAM, NOI_DUNG_DAO_TAO,
  SO_TIEN_SI, SO_THAC_SI, SO_DAI_HOC, SO_CAO_DANG, SO_TRUNG_CAP, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_DAO_TAO_HOC_VIEN
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_NANG_LUC_SO_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_NANG_LUC_SO_HIS (
      ID                    NUMBER(19)    GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID             NUMBER(19)    NOT NULL,
      DON_VI_ID             NUMBER(19)    NOT NULL,
      NHOM_VI_TRI           NVARCHAR2(20) NOT NULL,
      TONG_SO_DIEN_DANH_GIA NUMBER(10)   DEFAULT 0 NOT NULL,
      TONG_SO_DAT           NUMBER(10)   DEFAULT 0 NOT NULL,
      TONG_SO_CHUA_DAT      NUMBER(10)   DEFAULT 0 NOT NULL,
      GHI_CHU               NVARCHAR2(2000),
      KY_BAO_CAO_CODE       NVARCHAR2(50) NOT NULL,
      SNAPSHOT_BATCH_ID     NUMBER(19),
      SNAPSHOT_CREATED_AT   TIMESTAMP     NOT NULL,
      CREATED_AT            TIMESTAMP     DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT            TIMESTAMP     DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY            NUMBER(19),
      UPDATED_BY            NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_NLS_HIS_DONVI_KY ON BIZ_NANG_LUC_SO_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_NANG_LUC_SO_HIS (
  SOURCE_ID, DON_VI_ID, NHOM_VI_TRI,
  TONG_SO_DIEN_DANH_GIA, TONG_SO_DAT, TONG_SO_CHUA_DAT, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, NHOM_VI_TRI,
  TONG_SO_DIEN_DANH_GIA, TONG_SO_DAT, TONG_SO_CHUA_DAT, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_NANG_LUC_SO
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_VAN_BAN_QPPL_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_VAN_BAN_QPPL_HIS (
      ID                      NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID               NUMBER(19)      NOT NULL,
      DON_VI_ID               NUMBER(19)      NOT NULL,
      SO_HIEU                 NVARCHAR2(100)  NOT NULL,
      TEN_VAN_BAN             NVARCHAR2(500),
      LOAI_VAN_BAN            NVARCHAR2(100),
      CO_QUAN_BAN_HANH        NVARCHAR2(200),
      NGAY_BAN_HANH           DATE            NOT NULL,
      NGAY_HIEU_LUC           DATE,
      LINH_VUC                NVARCHAR2(100),
      TRICH_YEU               NVARCHAR2(2000),
      TINH_TRANG_TRIEN_KHAI   NVARCHAR2(50),
      GHI_CHU                 NVARCHAR2(2000),
      KY_BAO_CAO_CODE         NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID       NUMBER(19),
      SNAPSHOT_CREATED_AT     TIMESTAMP       NOT NULL,
      CREATED_AT              TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT              TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY              NUMBER(19),
      UPDATED_BY              NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_VBQP_HIS_DONVI_KY ON BIZ_VAN_BAN_QPPL_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_VAN_BAN_QPPL_HIS (
  SOURCE_ID, DON_VI_ID, SO_HIEU, TEN_VAN_BAN, LOAI_VAN_BAN, CO_QUAN_BAN_HANH,
  NGAY_BAN_HANH, NGAY_HIEU_LUC, LINH_VUC, TRICH_YEU, TINH_TRANG_TRIEN_KHAI, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, SO_HIEU, TEN_VAN_BAN, LOAI_VAN_BAN, CO_QUAN_BAN_HANH,
  NGAY_BAN_HANH, NGAY_HIEU_LUC, LINH_VUC, TRICH_YEU, TINH_TRANG_TRIEN_KHAI, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_VAN_BAN_QPPL
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_DU_AN_CNTT_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_DU_AN_CNTT_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      TEN_DU_AN           NVARCHAR2(500)  NOT NULL,
      DON_VI_CHU_TRI      NVARCHAR2(200),
      NAM_TRIEN_KHAI      NUMBER(4),
      NAM_DUA_VAO_SU_DUNG NUMBER(4),
      TONG_KINH_PHI       NUMBER(18, 3),
      NGUON_VON           NVARCHAR2(50),
      GHI_CHU             NVARCHAR2(2000),
      KY_BAO_CAO_CODE     NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID   NUMBER(19),
      SNAPSHOT_CREATED_AT TIMESTAMP       NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_DACN_HIS_DONVI_KY ON BIZ_DU_AN_CNTT_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_DU_AN_CNTT_HIS (
  SOURCE_ID, DON_VI_ID, TEN_DU_AN, DON_VI_CHU_TRI,
  NAM_TRIEN_KHAI, NAM_DUA_VAO_SU_DUNG, TONG_KINH_PHI, NGUON_VON, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, TEN_DU_AN, DON_VI_CHU_TRI,
  NAM_TRIEN_KHAI, NAM_DUA_VAO_SU_DUNG, TONG_KINH_PHI, NGUON_VON, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_DU_AN_CNTT
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_GIAM_SAT_SOC_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_GIAM_SAT_SOC_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      LOAI_MANG           NVARCHAR2(50)   NOT NULL,
      LOP_GIAM_SAT        NVARCHAR2(50)   NOT NULL,
      CO_HE_THONG         NUMBER(1)       DEFAULT 0 NOT NULL,
      THUC_TRANG          NVARCHAR2(50),
      NAM_THANH_LAP       NUMBER(4),
      SO_NHAN_SU          NUMBER(10),
      CONG_CU_SU_DUNG     NVARCHAR2(500),
      SO_CANH_BAO_THANG   NUMBER(10),
      GHI_CHU             NVARCHAR2(2000),
      KY_BAO_CAO_CODE     NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID   NUMBER(19),
      SNAPSHOT_CREATED_AT TIMESTAMP       NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_GSSOC_HIS_DONVI_KY ON BIZ_GIAM_SAT_SOC_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_GIAM_SAT_SOC_HIS (
  SOURCE_ID, DON_VI_ID, LOAI_MANG, LOP_GIAM_SAT, CO_HE_THONG,
  THUC_TRANG, NAM_THANH_LAP, SO_NHAN_SU, CONG_CU_SU_DUNG, SO_CANH_BAO_THANG, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, LOAI_MANG, LOP_GIAM_SAT, CO_HE_THONG,
  THUC_TRANG, NAM_THANH_LAP, SO_NHAN_SU, CONG_CU_SU_DUNG, SO_CANH_BAO_THANG, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_GIAM_SAT_SOC
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_GIAM_SAT_NOC_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_GIAM_SAT_NOC_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      LOP_GIAM_SAT        NVARCHAR2(50)   NOT NULL,
      CO_NOC              NUMBER(1)       DEFAULT 0 NOT NULL,
      THUC_TRANG          NVARCHAR2(50),
      NAM_THANH_LAP       NUMBER(4),
      SO_NHAN_SU          NUMBER(10),
      GHI_CHU             NVARCHAR2(2000),
      KY_BAO_CAO_CODE     NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID   NUMBER(19),
      SNAPSHOT_CREATED_AT TIMESTAMP       NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_GSNOC_HIS_DONVI_KY ON BIZ_GIAM_SAT_NOC_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_GIAM_SAT_NOC_HIS (
  SOURCE_ID, DON_VI_ID, LOP_GIAM_SAT, CO_NOC,
  THUC_TRANG, NAM_THANH_LAP, SO_NHAN_SU, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, LOP_GIAM_SAT, CO_NOC,
  THUC_TRANG, NAM_THANH_LAP, SO_NHAN_SU, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_GIAM_SAT_NOC
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_ATTT_HTTT_VAN_HANH_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_ATTT_HTTT_VAN_HANH_HIS (
      ID                        NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID                 NUMBER(19)      NOT NULL,
      DON_VI_ID                 NUMBER(19)      NOT NULL,
      HTTT_ID                   NUMBER(19)      NOT NULL,
      CHU_QUAN                  NVARCHAR2(200),
      DON_VI_VAN_HANH           NVARCHAR2(200),
      CAP_DO_DE_XUAT            NVARCHAR2(20),
      TINH_TRANG_PHE_DUYET      NVARCHAR2(50),
      QUYET_DINH_PHE_DUYET      NVARCHAR2(200),
      QUY_CHE_ATTT              NVARCHAR2(200),
      DU_KIEN_NGAY_PHE_DUYET    DATE,
      DA_TRIEN_KHAI_PHUONG_AN   NUMBER(1)       DEFAULT 0 NOT NULL,
      DU_KIEN_NGAY_TRIEN_KHAI   DATE,
      KIEM_TRA_DANH_GIA         NVARCHAR2(500),
      GHI_CHU                   NVARCHAR2(2000),
      KY_BAO_CAO_CODE           NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID         NUMBER(19),
      SNAPSHOT_CREATED_AT       TIMESTAMP       NOT NULL,
      CREATED_AT                TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT                TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY                NUMBER(19),
      UPDATED_BY                NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_AHVH_HIS_DONVI_KY ON BIZ_ATTT_HTTT_VAN_HANH_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_ATTT_HTTT_VAN_HANH_HIS (
  SOURCE_ID, DON_VI_ID, HTTT_ID, CHU_QUAN, DON_VI_VAN_HANH, CAP_DO_DE_XUAT,
  TINH_TRANG_PHE_DUYET, QUYET_DINH_PHE_DUYET, QUY_CHE_ATTT,
  DU_KIEN_NGAY_PHE_DUYET, DA_TRIEN_KHAI_PHUONG_AN, DU_KIEN_NGAY_TRIEN_KHAI,
  KIEM_TRA_DANH_GIA, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, HTTT_ID, CHU_QUAN, DON_VI_VAN_HANH, CAP_DO_DE_XUAT,
  TINH_TRANG_PHE_DUYET, QUYET_DINH_PHE_DUYET, QUY_CHE_ATTT,
  DU_KIEN_NGAY_PHE_DUYET, DA_TRIEN_KHAI_PHUONG_AN, DU_KIEN_NGAY_TRIEN_KHAI,
  KIEM_TRA_DANH_GIA, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_ATTT_HTTT_VAN_HANH
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_ATTT_HTTT_DAU_TU_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_ATTT_HTTT_DAU_TU_HIS (
      ID                        NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID                 NUMBER(19)      NOT NULL,
      DON_VI_ID                 NUMBER(19)      NOT NULL,
      HTTT_ID                   NUMBER(19)      NOT NULL,
      CHU_QUAN                  NVARCHAR2(200),
      DON_VI_VAN_HANH           NVARCHAR2(200),
      CAP_DO_DE_XUAT            NVARCHAR2(20),
      NGAY_PHE_DUYET_HSDXCD     DATE,
      QUYET_DINH_PHE_DUYET      NVARCHAR2(200),
      DA_LONG_GHEP_THUYET_MINH  NUMBER(1)       DEFAULT 0 NOT NULL,
      GHI_CHU                   NVARCHAR2(2000),
      KY_BAO_CAO_CODE           NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID         NUMBER(19),
      SNAPSHOT_CREATED_AT       TIMESTAMP       NOT NULL,
      CREATED_AT                TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT                TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY                NUMBER(19),
      UPDATED_BY                NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_AHDT_HIS_DONVI_KY ON BIZ_ATTT_HTTT_DAU_TU_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_ATTT_HTTT_DAU_TU_HIS (
  SOURCE_ID, DON_VI_ID, HTTT_ID, CHU_QUAN, DON_VI_VAN_HANH, CAP_DO_DE_XUAT,
  NGAY_PHE_DUYET_HSDXCD, QUYET_DINH_PHE_DUYET, DA_LONG_GHEP_THUYET_MINH, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, HTTT_ID, CHU_QUAN, DON_VI_VAN_HANH, CAP_DO_DE_XUAT,
  NGAY_PHE_DUYET_HSDXCD, QUYET_DINH_PHE_DUYET, DA_LONG_GHEP_THUYET_MINH, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_ATTT_HTTT_DAU_TU
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_CAMERA_QUAN_LY_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_CAMERA_QUAN_LY_HIS (
      ID                    NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID             NUMBER(19)      NOT NULL,
      DON_VI_ID             NUMBER(19)      NOT NULL,
      NHOM_CAMERA           NVARCHAR2(50),
      TEN_DON_VI_DIA_CHI    NVARCHAR2(300)  NOT NULL,
      BUONG_GIAM_TRANG_BI_SL NUMBER(10)    DEFAULT 0 NOT NULL,
      BUONG_GIAM_TRANG_BI_TS NUMBER(10)    DEFAULT 0 NOT NULL,
      NHU_CAU_DAU_TU        NUMBER(10)      DEFAULT 0 NOT NULL,
      BAO_TRI               NUMBER(10)      DEFAULT 0 NOT NULL,
      SUA_CHUA              NUMBER(10)      DEFAULT 0 NOT NULL,
      SO_LAN_VI_PHAM        NUMBER(10)      DEFAULT 0 NOT NULL,
      KET_NOI_CHIA_SE       NVARCHAR2(200),
      HO_SO_CAP_DO_ATTT     NUMBER(10)      DEFAULT 0 NOT NULL,
      CB_CHUYEN_TRACH       NUMBER(10)      DEFAULT 0 NOT NULL,
      CB_KIEM_NHIEM         NUMBER(10)      DEFAULT 0 NOT NULL,
      CB_DIA_PHUONG         NUMBER(10)      DEFAULT 0 NOT NULL,
      DAO_TAO_BO            NUMBER(10)      DEFAULT 0 NOT NULL,
      DAO_TAO_NHU_CAU       NUMBER(10)      DEFAULT 0 NOT NULL,
      GHI_CHU               NVARCHAR2(2000),
      KY_BAO_CAO_CODE       NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID     NUMBER(19),
      SNAPSHOT_CREATED_AT   TIMESTAMP       NOT NULL,
      CREATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT            TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY            NUMBER(19),
      UPDATED_BY            NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_CQLY_HIS_DONVI_KY ON BIZ_CAMERA_QUAN_LY_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_CAMERA_QUAN_LY_HIS (
  SOURCE_ID, DON_VI_ID, NHOM_CAMERA, TEN_DON_VI_DIA_CHI,
  BUONG_GIAM_TRANG_BI_SL, BUONG_GIAM_TRANG_BI_TS, NHU_CAU_DAU_TU,
  BAO_TRI, SUA_CHUA, SO_LAN_VI_PHAM, KET_NOI_CHIA_SE, HO_SO_CAP_DO_ATTT,
  CB_CHUYEN_TRACH, CB_KIEM_NHIEM, CB_DIA_PHUONG, DAO_TAO_BO, DAO_TAO_NHU_CAU, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, NHOM_CAMERA, TEN_DON_VI_DIA_CHI,
  BUONG_GIAM_TRANG_BI_SL, BUONG_GIAM_TRANG_BI_TS, NHU_CAU_DAU_TU,
  BAO_TRI, SUA_CHUA, SO_LAN_VI_PHAM, KET_NOI_CHIA_SE, HO_SO_CAP_DO_ATTT,
  CB_CHUYEN_TRACH, CB_KIEM_NHIEM, CB_DIA_PHUONG, DAO_TAO_BO, DAO_TAO_NHU_CAU, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_CAMERA_QUAN_LY
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;

-- ============================================================
-- BIZ_CAMERA_THUC_TRANG_HIS
-- ============================================================
BEGIN
  EXECUTE IMMEDIATE '
    CREATE TABLE BIZ_CAMERA_THUC_TRANG_HIS (
      ID                  NUMBER(19)      GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
      SOURCE_ID           NUMBER(19)      NOT NULL,
      DON_VI_ID           NUMBER(19)      NOT NULL,
      NHOM_CAMERA         NVARCHAR2(50),
      TEN_HE_THONG        NVARCHAR2(300)  NOT NULL,
      CAU_HINH_IP         NUMBER(10)      DEFAULT 0 NOT NULL,
      CAU_HINH_ANALOG     NUMBER(10)      DEFAULT 0 NOT NULL,
      THUC_TRANG_IP       NUMBER(10)      DEFAULT 0 NOT NULL,
      THUC_TRANG_ANALOG   NUMBER(10)      DEFAULT 0 NOT NULL,
      CHU_DAU_TU          NVARCHAR2(200),
      NAM_DAU_TU          NUMBER(4),
      DUONG_TRUYEN        NVARCHAR2(50),
      PHAN_MEM            NVARCHAR2(200),
      LUU_TRU             NVARCHAR2(200),
      GHI_CHU             NVARCHAR2(2000),
      KY_BAO_CAO_CODE     NVARCHAR2(50)   NOT NULL,
      SNAPSHOT_BATCH_ID   NUMBER(19),
      SNAPSHOT_CREATED_AT TIMESTAMP       NOT NULL,
      CREATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      UPDATED_AT          TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
      CREATED_BY          NUMBER(19),
      UPDATED_BY          NUMBER(19)
    )';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/
BEGIN
  EXECUTE IMMEDIATE 'CREATE INDEX IDX_CTTR_HIS_DONVI_KY ON BIZ_CAMERA_THUC_TRANG_HIS (DON_VI_ID, KY_BAO_CAO_CODE)';
EXCEPTION WHEN OTHERS THEN IF SQLCODE != -955 THEN RAISE; END IF;
END;
/

INSERT INTO BIZ_CAMERA_THUC_TRANG_HIS (
  SOURCE_ID, DON_VI_ID, NHOM_CAMERA, TEN_HE_THONG,
  CAU_HINH_IP, CAU_HINH_ANALOG, THUC_TRANG_IP, THUC_TRANG_ANALOG,
  CHU_DAU_TU, NAM_DAU_TU, DUONG_TRUYEN, PHAN_MEM, LUU_TRU, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, SNAPSHOT_CREATED_AT,
  CREATED_AT, UPDATED_AT, CREATED_BY, UPDATED_BY
)
SELECT
  ID, DON_VI_ID, NHOM_CAMERA, TEN_HE_THONG,
  CAU_HINH_IP, CAU_HINH_ANALOG, THUC_TRANG_IP, THUC_TRANG_ANALOG,
  CHU_DAU_TU, NAM_DAU_TU, DUONG_TRUYEN, PHAN_MEM, LUU_TRU, GHI_CHU,
  KY_BAO_CAO_CODE, SNAPSHOT_BATCH_ID, NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP),
  NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), NVL(SNAPSHOT_CREATED_AT, SYSTIMESTAMP), CREATED_BY, UPDATED_BY
FROM BIZ_CAMERA_THUC_TRANG
WHERE IS_LATEST = 0 AND KY_BAO_CAO_CODE IS NOT NULL;
