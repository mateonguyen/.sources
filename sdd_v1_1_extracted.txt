 `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n `n TÀI LI?U THI?T K? H? TH?NG `n System Design Document (SDD) `n H? th?ng qu?n lý phát tri?n và ?ng d?ng CNTT trong Công an nhân dân (CAND) `n Phiên b?n: v0.1 – Draft `n Ngày t?o: 23/03/2026 `n Ðon v? ch? qu?n: C?c Công ngh? thông tin (H05), B? Công an `n Ph?m vi: Ph?n 1: T?ng quan thi?t k? `n Liên quan SRS: System_Requirements_Specification_v1.1.docx `n `n Thông tin tài li?u `n L?ch s? thay d?i `n Phiên b?n `n Ngày `n Mô t? `n Ngu?i th?c hi?n `n v0.1 `n 23/03/2026 `n T?o m?i – Ph?n 1: T?ng quan thi?t k? `n AI Draft `n 1. T?ng quan thi?t k? `n 1.1 M?c dích tài li?u `n Tài li?u SDD (System Design Document) này mô t? thi?t k? k? thu?t chi ti?t c?a H? th?ng qu?n lý phát tri?n và ?ng d?ng CNTT trong Công an nhân dân (CAND). Tài li?u du?c xây d?ng d?a trên yêu c?u trong System Requirements Specification (SRS) v1.1 và có th? dùng tr?c ti?p làm co s? d? tri?n khai code. `n Ð?i tu?ng d?c: `n Developer / Full-stack Engineer: Hi?u ki?n trúc, thi?t k? DB, API và cách t? ch?c module d? l?p trình. `n Tech Lead / Software Architect: Rà soát quy?t d?nh thi?t k?, d?m b?o tính nh?t quán và kh? nang m? r?ng. `n QA / Tester: Hi?u lu?ng nghi?p v? và API contract d? thi?t k? test case. `n DevOps: N?m c?u hình tri?n khai, backup, monitoring. `n Qu?n lý d? án / Khách hàng H05: Ki?m tra tính d?y d? và phù h?p v?i yêu c?u nghi?p v?. `n Ph?m vi tài li?u: `n Bao g?m: Ki?n trúc h? th?ng, thi?t k? DB, thi?t k? API, thi?t k? component, sequence diagram, b?o m?t, tri?n khai, x? lý l?i & logging. `n Không bao g?m: Source code, test case chi ti?t, user manual, k? ho?ch d? án. `n M?i liên h? v?i SRS: `n M?i quy?t d?nh thi?t k? trong SDD d?u tham chi?u d?n mã FR/BR/NFR tuong ?ng trong SRS v1.1. `n Th? t? module trong SDD (6.1–6.11) kh?p v?i th? t? module trong SRS. `n Khi SRS thay d?i, SDD c?n du?c c?p nh?t d?ng b? và ghi vào b?ng L?ch s? thay d?i. `n 1.2 Ki?n trúc h? th?ng (Architecture Overview) `n 1.2.1 So d? ki?n trúc t?ng th? `n H? th?ng áp d?ng ki?n trúc SPA (Single Page Application) + REST API + RDBMS, tri?n khai t?p trung t?i trung tâm d? li?u H05, trên m?ng n?i b? BCANet. Không có k?t n?i Internet. So d? ki?n trúc d?ng text: `n `n Hình 1.1: So d? ki?n trúc t?ng th? — H? th?ng tri?n khai t?p trung t?i Trung tâm d? li?u H05 trên n?n BCANet `n 1.2.2 Ki?n trúc phân l?p (Layered Architecture) `n H? th?ng áp d?ng ki?n trúc 4 l?p, d?m b?o tách bi?t m?i quan tâm (Separation of Concerns), d? test và d? b?o trì: `n L?p `n Công ngh? `n Trách nhi?m `n L?p 1 – Presentation (Frontend) `n Angular 17 SPA, Angular Router, NgRx, PrimeNG `n Hi?n th? UI, x? lý s? ki?n ngu?i dùng, g?i REST API qua HttpClient. Không ch?a business logic. Qu?n lý state c?c b? (component state) và state toàn c?c (Redux store cho auth, k? báo cáo hi?n t?i, thông báo). `n L?p 2 – Application (Backend Controllers) `n ASP.NET Core Controllers, Filters, Middleware, Action Filters `n Ti?p nh?n HTTP request, xác th?c JWT (AuthGuard), ki?m tra quy?n (RBACGuard), validate DTO (class-validator), g?i Service. KHÔNG ch?a business logic, ch? orchestrate. Tr? v? HTTP response chu?n hóa. `n L?p 3 – Domain (Business Logic) `n ASP.NET Core Services, Domain Entities, Business Rules `n Ch?a toàn b? business logic: x? lý k? báo cáo state machine, ràng bu?c liên module (BR-CROSS-01), validate nghi?p v?, tính toán s? li?u. G?i Repository d? truy c?p d? li?u. `n L?p 4 – Infrastructure (Data Access) `n EF Core Repositories, Oracle, MinIO, Serilog `n Th?c hi?n CRUD database qua EF Core. Qu?n lý transaction khi có thao tác liên b?ng.c file dính kèm trên File Server. Ghi audit log vào b?ng system_log. `n 1.2.3 Giao ti?p gi?a các l?p `n T? ? Ð?n `n Co ch? giao ti?p `n Presentation ? Application `n HTTP/HTTPS REST API (JSON). Frontend g?i JWT trong header Authorization: Bearer <token>. Backend response format chu?n hóa: { data, meta, error }. `n Application ? Domain `n G?i tr?c ti?p qua Dependency Injection (ASP.NET Core). Controller g?i Service. DTO validate FluentValidation. tru?c khi truy?n vào Service. `n Domain ? Infrastructure `n Repository pattern. Service g?i Repository interface. EF Core t? sinh SQL query. Transaction scope.action du?c qu?n lý trong Service khi c?n atomic operation. `n Cross-cutting concerns `n Logging (AuditInterceptor ghi m?i mutation), Exception Filter (chuy?n exception thành HTTP error chu?n), Rate limiting (ThrottlerGuard: 100 req/min/IP). `n 1.2.4 Lý do ch?n ki?n trúc `n Quy?t d?nh ki?n trúc `n Lý do `n SPA + REST API `n Phù h?p mô hình web n?i b?, không c?n SEO. Angular và ASP.NET Core deploy d?c l?p.êng. Developer pool l?n t?i Vi?t Nam (Angular + ASP.NET Core). `n T?p trung (Centralized) `n Phù h?p yêu c?u t?p trung d? li?u v? H05 (NFR-PERF-01). Không c?n d?ng b? phân tán. Ðon gi?n hóa backup và audit. BCANet có bandwidth d? cho ~50 concurrent users. `n Không microservices `n Quy mô ~50 concurrent users không c?n microservices. Monolithic ASP.NET Core phù h?p.d? debug, deploy và maintain v?i team nh?. Có th? tách module sau n?u c?n. `n Oracle Database 19c (RDBMS) `n ransaction d?m b?o toàn v?n d? li?u. Oracle mi?n phí, m?nh v? JSON n?u c?n m? r?ng. `n 1.3 Technology Stack `n 1.3.1 Stack Backend (ASP.NET Core 8) `n Tên thu vi?n / Công ngh? `n M?c dích `n Phiên b?n / Ghi chú `n ASP.NET Core 8 Web API `n Web framework chính `n LTS d?n Nov 2026 `n C# 12 `n Ngôn ng? l?p trình `n Type-safe, async/await `n ASP.NET Core Identity `n Qu?n lý user/role/claim + RBAC d?ng `n Built-in, tích h?p EF Core `n EF Core 8 + Oracle.EFCore `n ORM duy nh?t — KHÔNG làm migration `n Oracle.EntityFrameworkCore 8.x `n Flyway `n Qu?n lý DB schema migration `n SQL-based, DBA review `n FluentValidation 11 `n Validation DTO `n Built-in rule engine `n AutoMapper 12 `n DTO ? Entity mapping `n Profile-based `n Hangfire `n Background jobs (luu state vào Oracle) `n Dashboard tích h?p `n IMemoryCache `n In-process cache, interface ICacheService `n S?n sàng swap Redis `n Serilog `n Structured logging ? file `n Rotate theo ngày, gi? 30 ngày `n Swashbuckle (Swagger UI) `n API documentation t? d?ng `n T? XML comment `n AWSSDK.S3 / Minio .NET SDK `n MinIO client (S3-compatible) `n File upload/download `n Gotenberg .NET client `n G?i Gotenberg API d? render HTML?PDF `n Container riêng `n ASP.NET Core Health Checks `n Endpoint /health cho CI/CD + Docker `n Built-in `n ASP.NET Core RateLimiter `n Rate limit endpoint login `n Built-in .NET 7+ `n 1.3.2 Stack Frontend (Angular 17) `n Tên thu vi?n / Công ngh? `n M?c dích `n Phiên b?n / Ghi chú `n Angular 17 (Standalone API) `n SPA framework `n TypeScript-first `n PrimeNG 17 `n UI Component Library toàn di?n `n Table, Form, Dialog, Chart... `n PrimeFlex `n CSS utility (Flexbox/Grid) — dùng k?t h?p v?i Tailwind `n Ði kèm PrimeNG `n TailwindCSS 3 `n Utility-first CSS — custom styling ngoài PrimeNG `n v3.x — tích h?p Angular CLI `n PrimeIcons `n Icon set `n Ði kèm PrimeNG `n NgRx 17 `n State management (Redux pattern) `n Store + Effects + DevTools `n RxJS 7 `n Reactive programming `n Observables, operators `n Angular Reactive Forms `n Form handling và validation `n Thu?n Angular, không Formly `n ngx-echarts `n Bi?u d? th?ng kê `n Apache ECharts 5 wrapper `n ExcelJS 4 `n Xu?t báo cáo Excel client-side () `n `n dayjs `n X? lý date/time `n Thay moment.js `n Angular CDK `n Virtual scroll, overlay `n Built-in Angular `n 1.3.3 Stack Database & Storage `n Tên thu vi?n / Công ngh? `n M?c dích `n Phiên b?n / Ghi chú `n Oracle Database 19c `n RDBMS chính (ACID, TDE, RMAN) `n LTS d?n 2027 `n Flyway `n Schema migration SQL-based `n Versioned migrations `n Oracle RMAN `n Backup/restore `n Full + incremental `n MinIO `n Object Storage S3-compatible `n Self-hosted, n?i m?ng `n Oracle SQL Developer `n Tool DBA H05 () `n `n 1.3.4 Stack Infrastructure & DevOps `n Tên thu vi?n / Công ngh? `n M?c dích `n Phiên b?n / Ghi chú `n Docker + Docker Compose `n Containerization (Dev + Staging) () `n `n GitLab CE (self-hosted H05) `n Source control + CI/CD pipeline () `n `n Nginx 1.25 `n Reverse proxy + serve Angular static `n TLS termination `n Gotenberg `n PDF rendering container (HTML?PDF) `n Docker image gotenberg/gotenberg `n Prometheus + Grafana `n Monitoring & alerting `n Self-hosted `n RHEL 8 / Ubuntu 22.04 `n OS server production `n Approved by H05 `n .env file `n Secret management (Docker --env-file) `n Không commit vào git `n 1.3.5 Stack Testing `n Tên thu vi?n / Công ngh? `n M?c dích `n Phiên b?n / Ghi chú `n xUnit + Moq + FluentAssertions `n Unit testing Backend () `n `n WebApplicationFactory `n Integration testing API `n Oracle test schema `n Cypress 13 `n E2E testing () `n `n k6 `n Performance testing () `n `n OWASP ZAP `n Security scanning () `n `n Jest + Angular Testing Library `n Unit testing Frontend () `n `n 1.4 Công ngh? B? sung Ð? xu?t Nghiên c?u `n H?ng m?c `n Công ngh? d? xu?t `n Thay th? / So sánh `n P `n Lý do c?n quy?t d?nh `n Identity & Auth `n ASP.NET Core Identity `n Keycloak (n?ng hon) `n P1 `n Qu?n lý user/role/claim built-in. Tích h?p s?n v?i EF Core. `n Identity & Auth `n JWT + Refresh Token (t? tri?n khai) `n OpenIddict, IdentityServer `n P1 `n Ðã quy?t d?nh trong SDD §3.2. Xác nh?n không dùng OAuth2/OIDC. `n Caching `n IMemoryCache (in-process) `n Redis (distributed cache) `n P2 `n N?u scale multi-instance ? c?n Redis. Single instance ? MemoryCache d?. `n Caching `n Redis 7 (self-hosted) `n Valkey (open-source fork Redis) `n P2 `n Cache session, rate limiting state, danh m?c. Nghiên c?u n?u c?n scale. `n Background Jobs `n Hangfire (self-hosted dashboard) `n Quartz.NET, .NET BackgroundService `n P2 `n C?n cho: g?i thông báo, xu?t báo cáo n?ng async, backup trigger. `n Real-time `n SignalR (ASP.NET Core built-in) `n WebSocket thu?n, SSE `n P2 `n Push thông báo real-time khi có b?n ghi m?i n?p / phê duy?t. `n Email n?i b? `n MailKit (SMTP) `n SendGrid (c?n Internet) `n P1 `n H05 có SMTP server n?i m?ng không? Xác nh?n tru?c khi thi?t k? notification. `n Logging `n Serilog ? file (rolling) `n ELK Stack (Elasticsearch+Kibana) `n P2 `n ELK m?nh hon nhung n?ng. File log d? cho giai do?n d?u. Có th? nâng c?p sau. `n Logging `n Seq (self-hosted log server) `n ELK, Loki+Grafana `n P3 `n Structured log viewer d? dùng hon ELK. Nghiên c?u n?u c?n search log. `n Health Check `n ASP.NET Core Health Checks `n Custom endpoint `n P1 `n C?n cho CI/CD smoke test và Docker health check. Built-in, không c?n cài thêm. `n API Docs `n Swagger UI (Swashbuckle) `n NSwag, Redoc `n P1 `n T? d?ng t? XML comment. C?n cho team Frontend và UAT. `n Unit Test `n xUnit + Moq + FluentAssertions `n NUnit, MSTest `n P1 `n xUnit là standard .NET. FluentAssertions: readable assertions. `n Integration Test `n WebApplicationFactory + Testcontainers `n MockHttp `n P2 `n Testcontainers: spin up Oracle container th?t trong CI pipeline. `n E2E Test `n Cypress 13 `n Playwright `n P2 `n Cypress d? vi?t, debug t?t. Playwright nhanh hon nhung cú pháp ph?c t?p hon. `n Performance Test `n k6 `n JMeter, Gatling `n P2 `n k6: script b?ng JS, CI/CD integration t?t, light-weight. `n DB Migration `n EF Core Migrations (dotnet ef) `n Flyway, Liquibase `n P1 `n EF Core built-in. Flyway/Liquibase n?u DBA mu?n SQL-first thay code-first. `n DB Management `n Oracle SQL Developer `n DBeaver (da n?n t?ng) `n P1 `n H05 dã có Oracle SQL Developer? Xác nh?n tool DBA dùng. `n Angular State `n NgRx Signals (m?i) thay Store `n NgRx Store truy?n th?ng `n P2 `n Angular 17+ Signals API nh? hon Store. Phù h?p v?i scope nh?/v?a. `n Table/Grid `n Angular Material Table `n AG Grid Community `n P2 `n AG Grid m?nh hon (virtual scroll, filter) nhung c?n license Pro n?u dùng tính nang nâng cao. `n Form builder `n Angular Reactive Forms `n Formly (dynamic forms) `n P2 `n Formly h?u ích n?u c?n sinh form d?ng t? config JSON (17 module nghi?p v? có c?u trúc tuong t?). `n PDF Export `n jsPDF + html2canvas `n Server-side PDF (PuppeteerSharp) `n P3 `n Xu?t PDF phi?u/báo cáo. Client-side (jsPDF) d? nhung kém layout ph?c t?p. `n Secret Management `n Environment variables + .env (Docker) `n HashiCorp Vault, Azure Key Vault `n P1 `n Vault t?t hon nhung c?n infrastructure thêm. Env var d? cho single-server. `n TLS Certificate `n Self-signed cert (H05 CA) `n Let's Encrypt (c?n Internet) `n P1 `n BCANet n?i m?ng ? dùng CA n?i b? H05. Xác nh?n H05 có CA server không. `n APM `n Prometheus + Grafana `n Elastic APM, Datadog (cloud) `n P2 `n Prometheus/Grafana self-hosted phù h?p môi tru?ng không Internet. `n Error tracking `n Sentry (self-hosted) `n Rollbar, Raygun `n P3 `n Track JavaScript/C# exception real-time. Sentry có b?n self-hosted free. `n H?ng m?c `n Công ngh? `n Phiên b?n `n Layer `n Lý do ch?n `n Frontend Framework `n React `n 18.3 `n Frontend `n Ph? bi?n nh?t VN, tài li?u d?y d?, nhi?u thu vi?n h? tr?. SPA phù h?p web n?i b?. `n UI Component Library `n PrimeNG `n 5.x `n Frontend `n B? component d?y d? (Table, Form, Modal, Notification). H? tr? ti?ng Vi?t t?t. `n State Management `n NgRx Store `n 2.x `n Frontend `n Qu?n lý auth token, k? báo cáo hi?n t?i, thông báo toàn c?c. `n HTTP Client `n Angular HttpClient `n 1.x `n Frontend `n Interceptor d? t? d?ng g?n JWT header và x? lý 401 refresh token. `n Build Tool `n Vite `n 5.x `n Frontend `n Build nhanh hon CRA/Webpack. Hot reload t?c thì trong dev. `n Routing `n Angular Router `n 6.x `n Frontend `n Nested routes, lazy loading per module. `n Form Validation `n Angular Reactive Forms `n 7.x / 3.x `n Frontend `n Validate ph?c t?p phía client, tích h?p t?t v?i PrimeNG. `n Backend Framework `n ASP.NET Core `n 10.x `n Backend `n TypeScript native, Module/Controller/Service pattern rõ ràng. Guard/Interceptor/Pipe built-in. `n Runtime `n Node.js `n 20 LTS `n Backend `n LTS dài h?n, tuong thích ASP.NET Core. Async/await phù h?p REST API. `n ORM `n EF Core `n 0.3.x `n Backend `n EF Core tích h?p s?n ASP.NET Core. Flyway qu?n lý migration. Oracle d?y d?. `n Validation `n class-validator + class-transformer `n 0.14 `n Backend `n Validate DTO qua FluentValidation, tích h?p v?i ASP.NET Core pipeline. `n Authentication `n jsonwebtoken + @nestjs/jwt `n 9.x `n Backend `n JWT access token (2h) + refresh token (7 ngày). Không dùng session/cookie. `n Password Hashing `n bcrypt `n 5.x `n Backend `n Hashing m?t kh?u v?i salt rounds=12. Ch?m có ch? ý d? ch?ng brute-force. `n Excel Export `n ExcelJS `n 4.x `n Backend `n T?o file .xlsx v?i formatting, merge cell. H? tr? ti?ng Vi?t UTF-8. `n Excel Import `n xlsx (SheetJS) `n 0.18 `n Backend `n Parse file Excel upload, validate t?ng dòng tru?c khi commit DB. `n File Upload `n Multer (@nestjs/platform-express) `n 1.x `n Backend `n Middleware upload file, validate MIME type, gi?i h?n 25MB. `n Database `n Oracle `n 16.x `n Database `n ACID, Foreign Key, Index, JSON column. Mi?n phí, ?n d?nh, pool developer VN. `n DB Migration `n Flyway Migration CLI `n 0.3.x `n Database `n Qu?n lý schema version, rollback n?u l?i. Seed data qua migration. `n Connection Pooling `n pg (node-postgres) `n 8.x `n Database `n Pool size c?u hình qua Oracle connection pool. EF Core dùng ODP.NET internally. `n Web Server `n Nginx `n 1.26 `n Infrastructure `n Reverse proxy, serve Angular SPA static files, SSL termination. `n Process Manager `n PM2 `n 5.x `n Infrastructure `n Qu?n lý Node.js process, auto- restart khi crash, cluster mode n?u c?n. `n Logging `n Winston + nest-winston `n 3.x `n Infrastructure `n Ghi log v?i level (ERROR/WARN/INFO/DEBUG), rotation hàng ngày, JSON format. `n OS `n Ubuntu Server `n 22.04 LTS `n Infrastructure `n LTS 5 nam, driver hardware t?t, d? cài Oracle/Nginx. `n Unit Test `n Jest `n 29.x `n Testing `n xUnit + Moq. Mock Dependency Injection. Coverage report qua Coverlet. `n E2E Test `n Supertest + Jest `n 6.x `n Testing `n Test API endpoint th?c t? v?i DB test environment. `n Frontend Test `n Jest + Angular Testing Library `n 1.x `n Testing `n Unit test component React, mock API calls. `n B?ng du?i là k?t qu? quy?t d?nh chính th?c c?a H05. Ðây là baseline cho toàn b? development. `n Nhóm `n Quy?t d?nh `n Ghi chú `n Auth `n ASP.NET Core Identity `n + RBAC d?ng (permission per module per action) `n Cache `n IMemoryCache `n Interface ICacheService s?n d? swap Redis sau khi c?n scale `n Jobs `n Hangfire `n Luu job state vào Oracle. Dashboard t?i /hangfire (admin only) `n Real-time `n B? phase 1 `n Không dùng SignalR phase 1. Thông báo qua polling API `n Email `n Không dùng `n Thông báo n?i b? hoàn toàn trong h? th?ng (b?ng thong_bao) `n Logging `n Serilog ? file `n Rotate theo ngày, gi? 30 ngày. Tên file: app-{date}.log `n DB Migration `n Flyway `n EF Core ch? làm ORM — t?t migration. DBA H05 review SQL tru?c deploy `n Frontend UI `n PrimeNG 17 `n Table, form controls, dialog, chart... toàn b? t? PrimeNG `n Form `n Reactive Forms `n Thu?n Angular. Không dùng Formly phase 1 `n Monitoring `n Prometheus + Grafana `n Self-hosted n?i m?ng `n Secret `n .env file `n Docker --env-file. Không commit vào git. Backup riêng `n TLS `n Ch? h?i H05 `n H05 xác nh?n có CA server n?i b? không ? c?p cert cho domain `n PDF Export `n Gotenberg `n Docker container riêng. Render HTML template ? PDF. API /forms/chromium/convert/html `n 2. Thi?t k? Co s? d? li?u `n Database Oracle 19c du?c t? ch?c trong schema CAND_QLCNTT. Nguyên t?c c?t lõi: m?i b?n ghi nghi?p v? d?u g?n v?i c?p (don_vi_id, ky_bao_cao). T?t c? b?ng có audit fields (created_at, updated_at, created_by, updated_by) và soft delete (deleted_at). `n 2.1 Entity Relationship Diagram (ERD) `n ERD m?c logic th? hi?n t?t c? b?ng chính và quan h? gi?a chúng. Ký hi?u: ||--o{ (1:N), ||--|| (1:1), }o--o{ (N:M). Ð?nh nghia Mermaid du?i dây có th? render tr?c ti?p t?i mermaid.live: `n 2.1.2 ERD V?t lý chi ti?t (Physical ERD) `n Các so d? ERD v?t lý du?i dây du?c sinh t? d?ng t? schema th?c t?, th? hi?n rõ tên c?t, ki?u ràng bu?c PK/FK và quan h? UML Multiplicity. Ký hi?u: 1 = exactly-one, 0..1 = zero-or-one, 1..* = one-or-many, 0..* = zero-or-many. Audit fields (created_at, updated_at, created_by, deleted_at) ?n trong so d? d? gi?m nhi?u, ký hi?u b?ng dòng "...". `n Hình 2.1: ERD-D-01: Core & Auth `n `n So d? quan h? b?ng n?n t?ng: phân c?p don v?, ngu?i dùng, phân quy?n d?ng (Role/Permission). Container "Auth & Phân quy?n" nhóm các b?ng role, permission, user_role, role_permission. `n `n Hình 2.2: ERD-D-02: K? Báo cáo & Vòng d?i `n `n Vòng d?i k? báo cáo: ky_bao_cao ? ky_trang_thai_don_vi (tr?ng thái t?ng don v?) ? yeu_cau_bo_sung (lu?ng m? l?i k?). `n `n Hình 2.3: ERD-D-03: Module 6.1–6.3 — VBQPPL & Nhân l?c CNT `n `n Van b?n quy ph?m pháp lu?t (van_ban_qppl), nhân l?c CNTT (nhan_luc_cntt) và dào t?o b?i du?ng (dao_tao_boi_duong), d?u g?n (don_vi_id, ky_bao_cao). `n `n Hình 2.4: ERD-D-04: Module 6.4–6.6 — Ðào t?o & Phát tri?n NL `n `n Ðào t?o h?c viên (dao_tao_hoc_vien), nang l?c s? (nang_luc_so), dào t?o nu?c ngoài (dao_tao_nuoc_ngoai). `n `n Hình 2.5: ERD-D-05: Module 6.7 — Thi?t b? & HTTT `n `n Thi?t b? CNTT (thiet_bi_cntt), h? t?ng m?ng (ha_tang_mang) và h? th?ng thông tin (he_thong_thong_tin) — b?ng trung tâm cho các module ATTT. `n `n Hình 2.6: ERD-D-06: Module 6.8–6.9 — ATTT `n `n Giám sát SOC/NOC (giam_sat_soc, giam_sat_noc), ATTT HTTT v?n hành & d?u tu (attt_httt_van_hanh, attt_httt_dau_tu), gi?i pháp ATTT (giai_phap_attt). Ràng bu?c BR-CROSS-01: t?o HTTT t? d?ng t?o b?n ghi ATTT. `n `n Hình 2.7: ERD-D-07: Module 6.10–6.11 — Camera & D? án CNT `n `n H? th?ng camera (camera_quan_ly ? camera_thuc_trang) và d? án d?u tu CNTT (du_an_cntt). `n `n Hình 2.8: ERD-D-08: H? th?ng n?n t?ng `n `n Các b?ng h? th?ng: system_log (audit), file_dinh_kem, thong_bao (in-app), auto_save_draft, danh_muc (lookup). `n 2.2 Data Dictionary `n Mô t? chi ti?t t?ng b?ng, t?ng c?t. M?i b?ng nghi?p v? d?u có (don_vi_id, ky_bao_cao) và audit fields (created_at, updated_at, created_by, updated_by, deleted_at). Index d? xu?t ghi cu?i m?i b?ng. `n ? NHÓM 1: B?ng n?n t?ng `n B?ng: loai_don_vi `n Mô t?: Danh m?c lo?i don v? CAND (C?c, V?, Phòng PA, v.v.) `n Liên quan SRS: SRS §4.2 – FR-ADMIN-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính t? tang `n MA_LOAI `n VARCHAR2(20) `n NO `n UNIQUE `n Mã d?nh danh lo?i: BCA, CAT, CAH, v.v. `n TEN_LOAI `n VARCHAR2(100) `n NO `n Tên d?y d? lo?i don v? `n CAP_DON_VI `n NUMBER(5) `n NO `n 1 `n CHECK(1..5) `n C?p phân c?p: 1=B?, 2=T?ng C?c, 3=C?c, 4=Phòng, 5=Ð?i `n THU_TU `n NUMBER(5) `n NO `n 0 `n Th? t? hi?n th? trong dropdown `n IS_ACTIVE `n NUMBER(1) `n NO `n TRUE `n TRUE = dang ho?t d?ng `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n Index: UNIQUE(ma_loai) `n B?ng: don_vi `n Mô t?: Ðon v? CAND toàn qu?c. H? tr? cây phân c?p (parent_id). Soft-delete b?ng suffix _GT{YYYYMMDD} trên ma_don_vi. `n Liên quan SRS: SRS §4.2 – FR-ADMIN-01, BR-SYS-04 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n MA_DON_VI `n VARCHAR2(50) `n NO `n UNIQUE `n Mã don v?. Khi xóa m?m ? thêm _GT{YYYYMMDD} `n TEN_DON_VI `n VARCHAR2(200) `n NO `n Tên d?y d? don v? `n TEN_VIET_TAT `n VARCHAR2(50) `n YES `n NULL `n Tên vi?t t?t `n LOAI_DON_VI_ID `n NUMBER(10) `n NO `n FK ? LOAI_DON_VI.ID `n Lo?i don v? `n PARENT_ID `n NUMBER(10) `n YES `n NULL `n FK ? DON_VI.ID `n Ðon v? c?p cha (NULL = c?p cao nh?t) `n DIA_CHI `n VARCHAR2(500) `n YES `n NULL `n Ð?a ch? tr? s? `n IS_ACTIVE `n NUMBER(1) `n NO `n TRUE `n Tr?ng thái ho?t d?ng `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n Admin t?o `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n Admin c?p nh?t l?n cu?i `n Index: UNIQUE(ma_don_vi) WHERE deleted_at IS NULL; INDEX(loai_don_vi_id); INDEX(parent_id) `n B?ng: danh_muc `n Mô t?: B?ng danh m?c dùng chung cho toàn b? h? th?ng. Không t?o b?ng enum riêng cho t?ng lo?i. `n Liên quan SRS: SRS §4.2 – FR-ADMIN-02, Luu ý quan tr?ng `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n NHOM_CODE `n VARCHAR2(50) `n NO `n Nhóm danh m?c: TRINH_DO, CHUYEN_NGANH, LOAI_THIET_BI, TINH_TRANG_DU_AN, v.v. `n ITEM_CODE `n VARCHAR2(50) `n NO `n UNIQUE per nhom `n Mã item trong nhóm `n TEN_HIEN_THI `n VARCHAR2(200) `n NO `n Tên hi?n th? trên UI `n MO_TA `n CLOB `n YES `n NULL `n Mô t? chi ti?t (tùy ch?n) `n THU_TU `n NUMBER(5) `n NO `n 0 `n Th? t? s?p x?p trong dropdown `n IS_ACTIVE `n NUMBER(1) `n NO `n TRUE `n TRUE = dang s? d?ng `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n Index: INDEX(nhom_code, is_active); UNIQUE(nhom_code, item_code) `n B?ng: ky_bao_cao `n Mô t?: K? báo cáo (nam + quý). Trung tâm ki?m soát vòng d?i nh?p li?u toàn h? th?ng. `n Liên quan SRS: SRS §6.0 – FR-KY-01..05, BR-KY-01..03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n KY_CODE `n VARCHAR2(7) `n NO `n UNIQUE `n Format: 2024Q1, 2024Q2, 2024Q3, 2024Q4 `n NAM `n NUMBER(5) `n NO `n CHECK(>= 2024) `n Nam báo cáo `n QUY `n NUMBER(5) `n NO `n CHECK(1..4) `n Quý báo cáo `n TRANG_THAI `n VARCHAR2(20) `n NO `n CHUAN_BI `n ENUM: CHUAN_BI, DANG_MO, DA_DONG `n Tr?ng thái k? toàn h? th?ng `n NGAY_BAT_DAU `n DATE `n YES `n NULL `n Ngày m? k? nh?p li?u `n NGAY_KET_THUC `n DATE `n YES `n NULL `n Ngày dóng k? `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú khi t?o/dóng k? `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n H05 Admin t?o k? `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n H05 Admin c?p nh?t k? `n Index: UNIQUE(ky_code); INDEX(trang_thai); INDEX(nam, quy) `n B?ng: ky_trang_thai_don_vi `n Mô t?: Tr?ng thái nh?p li?u c?a t?ng don v? trong t?ng k?. B?ng junction qu?n lý vòng d?i c?p don v?. `n Liên quan SRS: SRS §6.0 – FR-KY-04, BR-KY-02 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n KY_BAO_CAO_ID `n NUMBER(10) `n NO `n FK ? KY_BAO_CAO.ID `n K? báo cáo `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? CAND `n TRANG_THAI `n VARCHAR2(30) `n NO `n CHUA_NHAP `n ENUM: CHUA_NHAP, DANG_NHAP, DA_XAC_NHAN, DANG_BO_SUNG `n Tr?ng thái nh?p li?u `n NGAY_XAC_NHAN `n TIMESTAMP `n YES `n NULL `n Th?i di?m don v? xác nh?n hoàn thành `n CONFIRMED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User xác nh?n hoàn thành `n NGAY_MO_LAI `n TIMESTAMP `n YES `n NULL `n Th?i di?m H05 m? l?i k? cho don v? `n MO_LAI_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n H05 user m? l?i `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n Index: UNIQUE(ky_bao_cao_id, don_vi_id); INDEX(don_vi_id, trang_thai) `n ? NHÓM 2: Xác th?c & Phân quy?n (Auth / RBAC) `n B?ng: users `n Mô t?: Tài kho?n ngu?i dùng h? th?ng. Xác th?c d?c l?p b?ng username/password (không LDAP/SSO). `n Liên quan SRS: SRS §5.1 – FR-AUTH-01..05 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n USERNAME `n VARCHAR2(100) `n NO `n UNIQUE `n Tên dang nh?p, vi?t thu?ng, không d?u `n PASSWORD_HASH `n VARCHAR2(200) `n NO `n bcrypt hash, salt rounds=12 `n HO_TEN `n VARCHAR2(200) `n NO `n H? và tên d?y d? `n EMAIL `n VARCHAR2(200) `n YES `n NULL `n UNIQUE nullable `n Email n?i b? (tùy ch?n, không dùng g?i mail) `n SO_DIEN_THOAI `n VARCHAR2(20) `n YES `n NULL `n S? di?n tho?i n?i b? `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? công tác `n MUST_CHANGE_PASSWORD `n NUMBER(1) `n NO `n TRUE `n TRUE = b?t bu?c d?i m?t kh?u khi dang nh?p l?n d?u `n IS_ACTIVE `n NUMBER(1) `n NO `n TRUE `n FALSE = tài kho?n b? khóa `n LAST_LOGIN_AT `n TIMESTAMP `n YES `n NULL `n L?n dang nh?p g?n nh?t `n REFRESH_TOKEN_HASH `n VARCHAR2(200) `n YES `n NULL `n Hash c?a refresh token hi?n t?i (NULL = dã logout) `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n CREATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n Admin t?o tài kho?n `n Index: UNIQUE(username) WHERE deleted_at IS NULL; INDEX(don_vi_id); INDEX(is_active) `n B?ng: role `n Mô t?: Vai trò trong h? th?ng. Role có th? là system role (H05_ADMIN, DON_VI_USER) ho?c role tùy ch?nh. `n Liên quan SRS: SRS §5.2 – FR-RBAC-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n ROLE_CODE `n VARCHAR2(50) `n NO `n UNIQUE `n Mã role: H05_ADMIN, DON_VI_ADMIN, DON_VI_USER, VIEWER `n TEN_ROLE `n VARCHAR2(100) `n NO `n Tên vai trò hi?n th? `n MO_TA `n CLOB `n YES `n NULL `n Mô t? quy?n c?a role này `n IS_SYSTEM `n NUMBER(1) `n NO `n FALSE `n TRUE = role h? th?ng, không du?c xóa `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t `n CREATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n Admin t?o role `n B?ng: permission `n Mô t?: Quy?n chi ti?t trên t?ng module và action. Qu?n lý RBAC d?ng. `n Liên quan SRS: SRS §5.2 – FR-RBAC-02 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n PERM_CODE `n VARCHAR2(100) `n NO `n UNIQUE `n Mã quy?n: module_6_1:read, module_6_1:write, ky_bao_cao:manage `n MODULE `n VARCHAR2(50) `n NO `n Tên module: module_6_1, ky_bao_cao, user_mgmt, report, admin `n ACTION `n VARCHAR2(20) `n NO `n Hành d?ng: read, write, delete, export, import, manage `n MO_TA `n CLOB `n YES `n NULL `n Mô t? quy?n `n Index: UNIQUE(perm_code); INDEX(module) `n B?ng: role_permission `n Mô t?: B?ng junction gán quy?n cho role. Nhi?u-nhi?u gi?a role và permission. `n Liên quan SRS: SRS §5.2 – FR-RBAC-02 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ROLE_ID `n NUMBER(10) `n NO `n FK ? ROLE.ID, PK `n Vai trò `n PERMISSION_ID `n NUMBER(10) `n NO `n FK ? PERMISSION.ID, PK `n Quy?n du?c gán `n Index: PK(role_id, permission_id); INDEX(permission_id) `n B?ng: user_role `n Mô t?: Gán role cho user t?i m?t don v? c? th?. Cho phép user có role khác nhau ? các don v? khác nhau. `n Liên quan SRS: SRS §5.2 – FR-RBAC-03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n USER_ID `n NUMBER(10) `n NO `n FK ? USERS.ID, PK(3) `n User du?c gán `n ROLE_ID `n NUMBER(10) `n NO `n FK ? ROLE.ID, PK(3) `n Role du?c gán `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID, PK(3) `n Ph?m vi áp d?ng role `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m gán `n CREATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n Admin gán role `n Index: PK(user_id, role_id, don_vi_id) `n ? NHÓM 3: Module nghi?p v? (6.1–6.11) `n B?ng: van_ban_qppl `n Mô t?: Module 6.1 – Van b?n quy ph?m pháp lu?t CNTT. M?i dòng là m?t van b?n. `n Liên quan SRS: SRS §6.1 – FR-6.1-01..04 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n SO_HIEU `n VARCHAR2(100) `n NO `n S? hi?u van b?n `n TEN_VAN_BAN `n CLOB `n NO `n Tên/tiêu d? van b?n `n LOAI_VAN_BAN `n VARCHAR2(50) `n NO `n FK value in danh_muc(LOAI_VAN_BAN) `n Lo?i: Lu?t, Ngh? d?nh, Thông tu, Quy?t d?nh `n CO_QUAN_BAN_HANH `n VARCHAR2(200) `n NO `n Co quan ban hành `n NGAY_BAN_HANH `n DATE `n NO `n Ngày ban hành `n NGAY_HIEU_LUC `n DATE `n YES `n NULL `n Ngày có hi?u l?c (NULL = chua có) `n LINH_VUC `n VARCHAR2(100) `n YES `n NULL `n Linh v?c áp d?ng `n TOM_TAT `n CLOB `n YES `n NULL `n Tóm t?t n?i dung `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú b? sung `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(loai_van_ban); INDEX(ngay_ban_hanh) `n B?ng: nhan_luc_cntt `n Mô t?: Module 6.2 – Nhân l?c CNTT. M?i dòng là m?t cán b? CNTT c?a don v?. `n Liên quan SRS: SRS §6.2 – FR-6.2-01..04 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n HO_TEN `n VARCHAR2(200) `n NO `n H? và tên cán b? `n NGAY_SINH `n DATE `n YES `n NULL `n Ngày sinh `n GIOI_TINH `n VARCHAR2(10) `n YES `n NULL `n ENUM: Nam, N? `n Gi?i tính `n CHUC_VU `n VARCHAR2(200) `n YES `n NULL `n Ch?c v? / ch?c danh `n TRINH_DO `n VARCHAR2(50) `n NO `n FK value in danh_muc(TRINH_DO) `n Trình d?: Ti?n si, Th?c si, Ð?i h?c, Cao d?ng `n CHUYEN_NGANH `n VARCHAR2(100) `n YES `n NULL `n Chuyên ngành dào t?o `n NAM_KINH_NGHIEM `n NUMBER(5) `n YES `n NULL `n CHECK(>= 0) `n S? nam kinh nghi?m CNTT `n CHUNG_CHI `n CLOB `n YES `n NULL `n Ch?ng ch? CNTT dang có (JSON array ho?c text) `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(trinh_do) `n B?ng: dao_tao_boi_duong `n Mô t?: Module 6.3 – Ðào t?o b?i du?ng cán b? CNTT trong nu?c. M?i dòng là m?t khóa dào t?o. `n Liên quan SRS: SRS §6.3 – FR-6.3-01..04 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n TEN_KHOA_HOC `n VARCHAR2(300) `n NO `n Tên khóa h?c / b?i du?ng `n DON_VI_TO_CHUC `n VARCHAR2(200) `n NO `n Ðon v? t? ch?c dào t?o `n HINH_THUC `n VARCHAR2(50) `n NO `n FK value in danh_muc(HINH_THUC_DAO_TAO) `n T?p trung, Tr?c tuy?n, K?t h?p `n SO_LUONG_HV `n NUMBER(10) `n NO `n CHECK(> 0) `n S? lu?ng h?c viên tham gia `n THOI_GIAN_TU `n DATE `n NO `n Ngày b?t d?u khóa h?c `n THOI_GIAN_DEN `n DATE `n NO `n Ngày k?t thúc khóa h?c `n KINH_PHI `n NUMBER(15,0) `n YES `n NULL `n CHECK(>= 0) `n Kinh phí (VNÐ) `n NGUON_KINH_PHI `n VARCHAR2(100) `n YES `n NULL `n Ngu?n kinh phí `n KET_QUA `n VARCHAR2(50) `n YES `n NULL `n FK value in danh_muc(KET_QUA_DAO_TAO) `n Ð?t, Không d?t, Ðang h?c `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(hinh_thuc) `n B?ng: dao_tao_hoc_vien `n Mô t?: Module 6.4 – Ðào t?o h?c viên CNTT t?i các tru?ng CAND. M?i dòng là m?t khóa/l?p. `n Liên quan SRS: SRS §6.4 – FR-6.4-01..03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n TEN_TRUONG `n VARCHAR2(200) `n NO `n Tên tru?ng CAND t? ch?c dào t?o `n TEN_KHOA_HOC `n VARCHAR2(300) `n NO `n Tên khóa h?c / chuong trình `n TRINH_DO `n VARCHAR2(50) `n NO `n FK value in danh_muc(TRINH_DO) `n Trình d? dào t?o: Ð?i h?c, Cao d?ng, TCCN `n SO_LUONG_TUYEN `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? lu?ng tuy?n sinh `n SO_LUONG_TOT_NGHIEP `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? lu?ng t?t nghi?p `n NAM_HOC `n NUMBER(5) `n NO `n CHECK(>= 2000) `n Nam h?c `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: nang_luc_so `n Mô t?: Module 6.5 – Nang l?c s? cán b? chi?n si (không ph?i cán b? CNTT chuyên trách). `n Liên quan SRS: SRS §6.5 – FR-6.5-01..03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n TONG_CBCS `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n T?ng s? cán b? chi?n si c?a don v? `n SO_CO_MAY_TINH `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? CBCS có máy tính làm vi?c `n SO_SU_DUNG_PHAN_MEM `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? CBCS s? d?ng ph?n m?m nghi?p v? thu?ng xuyên `n SO_DA_TAP_HUAN `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? CBCS dã t?p hu?n k? nang s? trong k? `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: UNIQUE(don_vi_id, ky_bao_cao) WHERE deleted_at IS NULL (1 b?n ghi/don v?/k?) `n B?ng: dao_tao_nuoc_ngoai `n Mô t?: Module 6.6 – Ðào t?o, b?i du?ng CNTT ? nu?c ngoài. `n Liên quan SRS: SRS §6.6 – FR-6.6-01..03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n HO_TEN_HOC_VIEN `n VARCHAR2(200) `n NO `n H? tên h?c viên `n TEN_KHOA_HOC `n VARCHAR2(300) `n NO `n Tên khóa h?c / chuong trình `n QUOC_GIA `n VARCHAR2(100) `n NO `n Qu?c gia dào t?o `n CO_SO_DAO_TAO `n VARCHAR2(300) `n YES `n NULL `n Co s? dào t?o nu?c ngoài `n THOI_GIAN_TU `n DATE `n NO `n Ngày b?t d?u `n THOI_GIAN_DEN `n DATE `n NO `n Ngày k?t thúc `n KINH_PHI `n NUMBER(15,0) `n YES `n NULL `n Kinh phí (VNÐ quy d?i) `n NOI_DUNG_DAO_TAO `n CLOB `n YES `n NULL `n N?i dung / chuyên d? dào t?o `n KET_QUA `n VARCHAR2(50) `n YES `n NULL `n FK value in danh_muc(KET_QUA_DAO_TAO) `n K?t qu? `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: thiet_bi_cntt `n Mô t?: Module 6.7 – Thi?t b? CNTT (máy tính, laptop, máy in, v.v.). M?i dòng là m?t lo?i thi?t b?. `n Liên quan SRS: SRS §6.7 – FR-6.7-01..05 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n LOAI_THIET_BI `n VARCHAR2(50) `n NO `n FK value in danh_muc(LOAI_THIET_BI) `n Lo?i: Máy tính d? bàn, Laptop, Máy in, Scanner, v.v. `n HANG_SAN_XUAT `n VARCHAR2(100) `n YES `n NULL `n Hãng s?n xu?t `n MODEL `n VARCHAR2(100) `n YES `n NULL `n Model thi?t b? `n SO_LUONG_TONG `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n T?ng s? lu?ng `n SO_LUONG_DUNG `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? lu?ng dang s? d?ng `n SO_LUONG_HONG `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? lu?ng hu h?ng `n NAM_DUA_VAO_SU_DUNG `n NUMBER(5) `n YES `n NULL `n Nam dua vào s? d?ng `n TINH_TRANG `n VARCHAR2(50) `n NO `n FK value in danh_muc(TINH_TRANG_THIET_BI) `n Tình tr?ng: T?t, Trung bình, H?ng `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(loai_thiet_bi, tinh_trang) `n B?ng: ha_tang_mang `n Mô t?: Module 6.7 – H? t?ng m?ng (du?ng truy?n, thi?t b? m?ng). M?i dòng là m?t lo?i h? t?ng. `n Liên quan SRS: SRS §6.7 – FR-6.7-06..08 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n LOAI_KET_NOI `n VARCHAR2(50) `n NO `n FK value in danh_muc(LOAI_KET_NOI) `n LAN, WAN, BCANet, Wifi `n BANG_THONG `n VARCHAR2(50) `n YES `n NULL `n Bang thông (Mbps/Gbps) `n NHA_CUNG_CAP `n VARCHAR2(100) `n YES `n NULL `n Nhà cung c?p d?ch v? `n SO_LUONG_DIEM `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? di?m/nút m?ng `n TINH_TRANG `n VARCHAR2(50) `n NO `n FK value in danh_muc(TINH_TRANG_HA_TANG) `n ?n d?nh, Chua ?n d?nh, H?ng `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: he_thong_thong_tin `n Mô t?: Module 6.7 – H? th?ng thông tin dang v?n hành. Khi t?o m?i, t? d?ng t?o b?n ghi ATTT (BR-CROSS-01). `n Liên quan SRS: SRS §6.7 – FR-6.7-09..12, BR-CROSS-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n TEN_HTTT `n VARCHAR2(300) `n NO `n Tên h? th?ng thông tin `n MA_HTTT `n VARCHAR2(50) `n YES `n NULL `n UNIQUE per don_vi `n Mã d?nh danh HTTT `n LOAI_HTTT `n VARCHAR2(50) `n NO `n FK value in danh_muc(LOAI_HTTT) `n Ph?n m?m nghi?p v?, CSDL, C?ng d?ch v?, v.v. `n DON_VI_PHAT_TRIEN `n VARCHAR2(200) `n YES `n NULL `n Ðon v? phát tri?n / cung c?p `n NAM_DUA_VAO_SU_DUNG `n NUMBER(5) `n YES `n NULL `n Nam dua vào s? d?ng `n TRANG_THAI `n VARCHAR2(30) `n NO `n FK value in danh_muc(TRANG_THAI_HTTT) `n Ðang v?n hành, T?m d?ng, Ng?ng `n SO_NGUOI_DUNG `n NUMBER(10) `n YES `n NULL `n CHECK(>= 0) `n S? ngu?i dùng hi?n t?i `n MO_TA `n CLOB `n YES `n NULL `n Mô t? ch?c nang chính `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(trang_thai). Trigger: Sau khi INSERT ? t? INSERT vào attt_httt_van_hanh và attt_httt_dau_tu (BR-CROSS-01) `n B?ng: giam_sat_soc `n Mô t?: Module 6.8 – Trung tâm giám sát an ninh m?ng SOC. Optional: don v? không có SOC du?c b? tr?ng. `n Liên quan SRS: SRS §6.8 – FR-6.8-01..03, BR-OPT-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n CO_SOC `n NUMBER(1) `n NO `n FALSE `n Ðon v? có SOC hay không `n NAM_THANH_LAP `n NUMBER(5) `n YES `n NULL `n Nam thành l?p SOC `n SO_NHAN_SU `n NUMBER(10) `n YES `n NULL `n CHECK(>= 0) `n S? nhân s? v?n hành SOC `n CONG_CU_SU_DUNG `n CLOB `n YES `n NULL `n Công c?/ph?n m?m SOC dang dùng `n SO_CANH_BAO_THANG `n NUMBER(10) `n YES `n NULL `n CHECK(>= 0) `n S? c?nh báo x? lý trong k? `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: UNIQUE(don_vi_id, ky_bao_cao) WHERE deleted_at IS NULL `n B?ng: giam_sat_noc `n Mô t?: Module 6.8 – Trung tâm giám sát h? t?ng m?ng NOC. Optional. `n Liên quan SRS: SRS §6.8 – FR-6.8-04..06, BR-OPT-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n CO_NOC `n NUMBER(1) `n NO `n FALSE `n Ðon v? có NOC hay không `n NAM_THANH_LAP `n NUMBER(5) `n YES `n NULL `n Nam thành l?p NOC `n SO_NHAN_SU `n NUMBER(10) `n YES `n NULL `n CHECK(>= 0) `n S? nhân s? v?n hành NOC `n PHAM_VI_GIAM_SAT `n CLOB `n YES `n NULL `n Ph?m vi giám sát (s? thi?t b?, bandwidth, v.v.) `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: attt_httt_van_hanh `n Mô t?: Module 6.9 – ATTT c?a HTTT dang v?n hành. T? d?ng t?o khi có HTTT m?i (BR-CROSS-01). `n Liên quan SRS: SRS §6.9 – FR-6.9-01..04, BR-CROSS-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n HTTT_ID `n NUMBER(10) `n NO `n FK ? HE_THONG_THONG_TIN.ID `n HTTT tuong ?ng (b?t bu?c) `n CAP_DO_ATTT `n VARCHAR2(20) `n NO `n FK value in danh_muc(CAP_DO_ATTT) `n C?p d? 1..5 theo TT17/2014 `n CO_KIEM_TRA `n NUMBER(1) `n NO `n FALSE `n Ðã ki?m tra/dánh giá ATTT trong k? `n NGAY_KIEM_TRA `n DATE `n YES `n NULL `n Ngày ki?m tra g?n nh?t `n DON_VI_KIEM_TRA `n VARCHAR2(200) `n YES `n NULL `n Ðon v? th?c hi?n ki?m tra `n KET_QUA `n CLOB `n YES `n NULL `n K?t qu? ki?m tra ATTT `n LO_HONG_PHAT_HIEN `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? l? h?ng phát hi?n `n LO_HONG_XU_LY `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? l? h?ng dã x? lý `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(httt_id); UNIQUE(httt_id, ky_bao_cao) `n B?ng: attt_httt_dau_tu `n Mô t?: Module 6.9 – Ð?u tu ATTT cho HTTT. Ngân sách, gi?i pháp mua s?m trong k?. `n Liên quan SRS: SRS §6.9 – FR-6.9-05..07 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n HTTT_ID `n NUMBER(10) `n NO `n FK ? HE_THONG_THONG_TIN.ID `n HTTT tuong ?ng `n HANG_MUC `n VARCHAR2(200) `n NO `n H?ng m?c d?u tu ATTT `n KINH_PHI `n NUMBER(15,0) `n NO `n 0 `n CHECK(>= 0) `n Kinh phí d?u tu (VNÐ) `n NGUON_KINH_PHI `n VARCHAR2(100) `n YES `n NULL `n Ngân sách nhà nu?c, d? án, v.v. `n TRANG_THAI `n VARCHAR2(30) `n NO `n FK value in danh_muc(TRANG_THAI_DAU_TU) `n K? ho?ch, Ðang th?c hi?n, Hoàn thành `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: giai_phap_attt `n Mô t?: Module 6.9 – Gi?i pháp ATTT t?ng th? c?a don v? (firewall, IDS, antivirus, v.v.). `n Liên quan SRS: SRS §6.9 – FR-6.9-08..10 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n LOAI_GIAI_PHAP `n VARCHAR2(100) `n NO `n FK value in danh_muc(LOAI_GIAI_PHAP_ATTT) `n Firewall, IDS/IPS, Antivirus, Backup, Mã hóa, v.v. `n TEN_SAN_PHAM `n VARCHAR2(200) `n YES `n NULL `n Tên s?n ph?m / gi?i pháp c? th? `n NHA_CUNG_CAP `n VARCHAR2(200) `n YES `n NULL `n Nhà cung c?p `n SO_LUONG `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? lu?ng license / thi?t b? `n NAM_DUA_VAO `n NUMBER(5) `n YES `n NULL `n Nam dua vào s? d?ng `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: camera_quan_ly `n Mô t?: Module 6.10 – Qu?n lý h? th?ng camera giám sát (m?c t?ng th? theo don v?). `n Liên quan SRS: SRS §6.10 – FR-6.10-01..04 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n HE_THONG_CAMERA `n VARCHAR2(200) `n NO `n Tên / mã h? th?ng camera `n SO_DIEM_LAP_DAT `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n S? di?m l?p d?t camera `n SO_CAMERA_TONG `n NUMBER(10) `n NO `n 0 `n CHECK(>= 0) `n T?ng s? camera `n CO_LUU_TRU_TAP_TRUNG `n NUMBER(1) `n NO `n FALSE `n Có luu tr? t?p trung v? máy ch? không `n NHA_CUNG_CAP `n VARCHAR2(200) `n YES `n NULL `n Nhà cung c?p h? th?ng `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: camera_thuc_trang `n Mô t?: Module 6.10 – Th?c tr?ng t?ng camera (chi ti?t c?p camera). FK v? camera_quan_ly. `n Liên quan SRS: SRS §6.10 – FR-6.10-05..07 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n CAMERA_QUAN_LY_ID `n NUMBER(10) `n NO `n FK ? CAMERA_QUAN_LY.ID `n H? th?ng camera ch?a camera này `n VI_TRI `n VARCHAR2(300) `n NO `n V? trí l?p d?t `n LOAI_CAMERA `n VARCHAR2(50) `n NO `n FK value in danh_muc(LOAI_CAMERA) `n IP Camera, PTZ, Dome, Bullet `n DO_PHAN_GIAI `n VARCHAR2(20) `n YES `n NULL `n Ð? phân gi?i: 2MP, 4MP, 8MP `n TINH_TRANG `n VARCHAR2(30) `n NO `n FK value in danh_muc(TINH_TRANG_CAMERA) `n Ho?t d?ng t?t, H?ng, C?n b?o trì `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n B?ng: du_an_cntt `n Mô t?: Module 6.11 – D? án CNTT dang tri?n khai / k? ho?ch trong k?. `n Liên quan SRS: SRS §6.11 – FR-6.11-01..05 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n Format YYYYQQ `n K? báo cáo `n TEN_DU_AN `n VARCHAR2(300) `n NO `n Tên d? án `n MA_DU_AN `n VARCHAR2(50) `n YES `n NULL `n Mã d? án (n?u có) `n LOAI_DU_AN `n VARCHAR2(50) `n NO `n FK value in danh_muc(LOAI_DU_AN) `n Mua s?m, Phát tri?n, Thuê d?ch v?, Nâng c?p `n TONG_MUC_DAU_TU `n NUMBER(18,0) `n YES `n NULL `n CHECK(>= 0) `n T?ng m?c d?u tu (VNÐ) `n NGUON_VON `n VARCHAR2(100) `n YES `n NULL `n Ngu?n v?n: NSNN, ODA, T? ch? `n NGAY_BAT_DAU `n DATE `n YES `n NULL `n Ngày b?t d?u `n NGAY_KET_THUC `n DATE `n YES `n NULL `n Ngày d? ki?n hoàn thành `n TRANG_THAI `n VARCHAR2(30) `n NO `n FK value in danh_muc(TINH_TRANG_DU_AN) `n K? ho?ch, Ðang tri?n khai, Hoàn thành, T?m d?ng `n DON_VI_THUC_HIEN `n VARCHAR2(200) `n YES `n NULL `n Ðon v? / nhà th?u th?c hi?n `n MO_TA `n CLOB `n YES `n NULL `n Mô t? m?c tiêu d? án `n GHI_CHU `n CLOB `n YES `n NULL `n Ghi chú `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o b?n ghi `n UPDATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m c?p nh?t l?n cu?i `n CREATED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User t?o b?n ghi `n UPDATED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n User c?p nh?t l?n cu?i `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete: NULL = chua xóa `n Index: INDEX(don_vi_id, ky_bao_cao); INDEX(trang_thai); INDEX(loai_du_an) `n ? NHÓM 4: B?ng h? th?ng `n B?ng: system_log `n Mô t?: Audit log m?i thao tác CREATE/UPDATE/DELETE trong h? th?ng. Ð?c bi?t ghi khi H05 s?a d? li?u don v? khác. `n Liên quan SRS: SRS §5.3 – NFR-AUDIT-01, NFR-AUDIT-02 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER(19) GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính (BIGINT d? không tràn) `n USER_ID `n NUMBER(10) `n NO `n FK ? USERS.ID `n User th?c hi?n thao tác `n DON_VI_ID `n NUMBER(10) `n YES `n NULL `n FK ? DON_VI.ID `n Ðon v? c?a d? li?u b? tác d?ng `n ACTION_TYPE `n VARCHAR2(20) `n NO `n ENUM: CREATE, UPDATE, DELETE, LOGIN, LOGOUT `n Lo?i thao tác `n TABLE_NAME `n VARCHAR2(100) `n YES `n NULL `n Tên b?ng b? tác d?ng `n RECORD_ID `n NUMBER(10) `n YES `n NULL `n ID b?n ghi b? tác d?ng `n BEFORE_DATA `n CLOB `n YES `n NULL `n D? li?u tru?c khi thay d?i (diff) `n AFTER_DATA `n CLOB `n YES `n NULL `n D? li?u sau khi thay d?i `n IP_ADDRESS `n VARCHAR2(45) `n YES `n NULL `n IP address c?a request `n USER_AGENT `n VARCHAR2(500) `n YES `n NULL `n User-Agent HTTP header `n LOGGED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m ghi log `n Index: INDEX(user_id); INDEX(don_vi_id); INDEX(logged_at); INDEX(action_type, table_name). Không có updated_at/deleted_at (log là immutable). `n B?ng: file_dinh_kem `n Mô t?: Qu?n lý file dính kèm cho các b?n ghi nghi?p v?. Max 25MB, h? tr? PDF/DOC/XLS. `n Liên quan SRS: SRS §5.4 – NFR-FILE-01..03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? s? h?u file `n ENTITY_TYPE `n VARCHAR2(50) `n NO `n Lo?i b?n ghi: van_ban_qppl, du_an_cntt, v.v. `n ENTITY_ID `n NUMBER(10) `n NO `n ID b?n ghi nghi?p v? tuong ?ng `n FILE_NAME `n VARCHAR2(200) `n NO `n Tên file g?c c?a ngu?i dùng `n FILE_PATH `n VARCHAR2(500) `n NO `n Ðu?ng d?n luu tr? trên File Server `n FILE_SIZE `n NUMBER(19) `n NO `n CHECK(<= 26214400) `n Kích thu?c file (bytes, max 25MB) `n MIME_TYPE `n VARCHAR2(100) `n NO `n MIME type: application/pdf, application/vnd.ms-excel, v.v. `n UPLOADED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User upload `n UPLOADED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m upload `n DELETED_AT `n TIMESTAMP `n YES `n NULL `n Soft delete `n Index: INDEX(don_vi_id); INDEX(entity_type, entity_id) WHERE deleted_at IS NULL `n B?ng: yeu_cau_bo_sung `n Mô t?: Yêu c?u m? l?i k? báo cáo t? don v? g?i lên H05. State machine 5 bu?c. `n Liên quan SRS: SRS §6.0 – FR-KY-06, BR-KY-03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n KY_BAO_CAO_ID `n NUMBER(10) `n NO `n FK ? KY_BAO_CAO.ID `n K? báo cáo c?n m? l?i `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? yêu c?u `n TRANG_THAI `n VARCHAR2(30) `n NO `n CHO_DUYET `n ENUM: CHO_DUYET, DA_DUYET, TU_CHOI, DANG_BO_SUNG, HOAN_THANH `n Tr?ng thái x? lý yêu c?u `n LY_DO `n CLOB `n NO `n Lý do yêu c?u m? l?i (b?t bu?c) `n REQUESTED_BY `n NUMBER(10) `n NO `n FK ? USERS.ID `n User don v? g?i yêu c?u `n REQUESTED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m g?i yêu c?u `n APPROVED_BY `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n H05 Admin duy?t/t? ch?i `n APPROVED_AT `n TIMESTAMP `n YES `n NULL `n Th?i di?m duy?t/t? ch?i `n TU_CHOI_LY_DO `n CLOB `n YES `n NULL `n Lý do t? ch?i (n?u b? t? ch?i) `n HAN_BO_SUNG `n DATE `n YES `n NULL `n H?n chót b? sung (H05 d?t khi duy?t) `n COMPLETED_AT `n TIMESTAMP `n YES `n NULL `n Th?i di?m don v? xác nh?n hoàn thành b? sung `n Index: INDEX(don_vi_id, ky_bao_cao_id); INDEX(trang_thai); INDEX(requested_at) `n B?ng: thong_bao `n Mô t?: Thông báo in-app cho ngu?i dùng. Ch? hi?n th? trên Dashboard, không email/SMS. `n Liên quan SRS: SRS §5.5 – NFR-NOTIFY-01 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n DON_VI_ID `n NUMBER(10) `n YES `n NULL `n FK ? DON_VI.ID `n NULL = g?i cho t?t c?; có giá tr? = g?i riêng don v? `n USER_ID `n NUMBER(10) `n YES `n NULL `n FK ? USERS.ID `n NULL = g?i c? don v?; có giá tr? = g?i riêng user `n LOAI_THONG_BAO `n VARCHAR2(30) `n NO `n FK value in danh_muc(LOAI_THONG_BAO) `n KY_MO, KY_DONG, YEU_CAU_DUYET, YEU_CAU_TU_CHOI, HAN_NHAP_LIEU `n TIEU_DE `n VARCHAR2(300) `n NO `n Tiêu d? thông báo `n NOI_DUNG `n CLOB `n NO `n N?i dung d?y d? `n DA_DOC `n NUMBER(1) `n NO `n FALSE `n TRUE = ngu?i dùng dã d?c `n DOC_LUC `n TIMESTAMP `n YES `n NULL `n Th?i di?m d?c `n LIEN_KY_ID `n NUMBER(10) `n YES `n NULL `n FK ? KY_BAO_CAO.ID `n K? báo cáo liên quan (n?u có) `n LIEN_YC_ID `n NUMBER(10) `n YES `n NULL `n FK ? YEU_CAU_BO_SUNG.ID `n Yêu c?u b? sung liên quan `n CREATED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m t?o thông báo `n Index: INDEX(don_vi_id, da_doc); INDEX(user_id, da_doc); INDEX(created_at DESC) `n B?ng: auto_save_draft `n Mô t?: Luu nháp t? d?ng m?i 5 phút. Khôi ph?c sau khi session h?t h?n và re-login. `n Liên quan SRS: SRS §5.6 – FR-DRAFT-01..03 `n C?t `n Ki?u d? li?u `n Nullable `n Default `n Constraint `n Mô t? `n ID `n NUMBER GENERATED BY DEFAULT AS IDENTITY `n NO `n PK `n Khóa chính `n USER_ID `n NUMBER(10) `n NO `n FK ? USERS.ID `n User dang nh?p li?u `n DON_VI_ID `n NUMBER(10) `n NO `n FK ? DON_VI.ID `n Ðon v? nh?p li?u `n MODULE_NAME `n VARCHAR2(50) `n NO `n Tên module: module_6_1, module_6_7, v.v. `n KY_BAO_CAO `n VARCHAR2(7) `n NO `n K? báo cáo dang nh?p `n DRAFT_DATA `n CLOB `n NO `n Toàn b? d? li?u form ? th?i di?m auto-save `n SAVED_AT `n TIMESTAMP `n NO `n NOW() `n Th?i di?m luu nháp g?n nh?t `n Index: UNIQUE(user_id, module_name, ky_bao_cao) — m?i user ch? có 1 draft/module/k?; upsert khi save `n 2.3 Chi?n lu?c Soft Delete `n H? th?ng áp d?ng hai co ch? xóa m?m khác nhau tùy lo?i b?ng: `n Lo?i b?ng `n Co ch? `n Chi ti?t `n Ví d? `n don_vi `n Suffix _GT{YYYYMMDD} trên ma_don_vi `n Thêm h?u t? vào mã don v?. Tên don v? gi? nguyên. Các b?n ghi nghi?p v? l?ch s? gi? nguyên FK. is_active = FALSE. `n ma_don_vi = "CAT01_GT20250315" `n B?ng nghi?p v? (van_ban_qppl, du_an_cntt, v.v.) `n C?t deleted_at TIMESTAMP `n NULL = b?n ghi dang t?n t?i. Có giá tr? = dã xóa. EF Core Global Query Filter t? d?ng l?c.NULL. EF Core @DeleteDateColumn() t? d?ng x? lý. `n deleted_at = "2025-03-15 10:00:00" `n users `n deleted_at + is_active = FALSE `n K?t h?p c? hai. deleted_at ghi nh?n th?i di?m xóa. is_active=FALSE d? login guard check nhanh. `n system_log, thong_bao `n KHÔNG xóa (immutable) `n Log và thông báo là immutable — không bao gi? xóa. Ch? thêm m?i. Rotation b?ng archive d?nh k?. `n Cách implement trong ASP.NET Core / EF Core: `n // Entity: b?ng nghi?p v? `n @Entity() `n export class VanBanQppl { `n @PrimaryGeneratedColumn() id: number; `n // EF Core: ISoftDelete interface + Global Query Filter `n } `n `n // Query: luôn filter deleted_at `n // EF Core: dbContext.Set<T>().IgnoreQueryFilters() // ch? ORM — Flyway qu?n lý schema d? xem c? b?n xóa (ch? H05 Admin) `n repo.find({ where: { don_vi_id, ky_bao_cao } }) // t? exclude deleted `n 2.4 Chi?n lu?c Migration & Seed Data `n 2.4.1 Qu?n lý Migration `n S? d?ng Flyway CLI d? qu?n lý schema version. M?i thay d?i DB d?u ph?i qua migration file, không s?a schema tr?c ti?p. `n # T?o migration m?i `n flyway migrate -- Flyway d?c V{version}__{description}.sql t? thu m?c migrations/ `n `n # Ch?y migration (t? d?ng trong startup n?u synchronize=false) `n flyway migrate -d src/data-source.ts `n `n # Rollback n?u l?i `n flyway repair # Ho?c: flyway migrate <PreviousMigration> -d src/data-source.ts `n `n # Xem danh sách migration dã ch?y `n flyway info -d src/data-source.ts `n Quy t?c Migration `n Chi ti?t `n Tên file `n Format: {timestamp}-{MoTa}.ts. Ví d?: 1710000000000-CreateDonVi.ts `n Không s?a migration dã ch?y `n N?u c?n s?a schema, t?o migration m?i. KHÔNG s?a file cu. `n Atomic migration `n M?i migration nên x? lý 1 thay d?i logic (1 b?ng ho?c 1 nhóm FK liên quan). `n Rollback `n M?i migration ph?i implement down() d? có th? revert. `n Môi tru?ng `n Migration ch?y t? d?ng khi deploy. C?n test migration trên staging tru?c production. `n Backup tru?c migration `n B?t bu?c backup DB tru?c khi ch?y migration trên production. `n 2.4.2 Seed Data cho b?ng danh_muc `n B?ng danh_muc c?n du?c seed s?n d? li?u khi kh?i t?o h? th?ng. Seed data du?c vi?t du?i d?ng migration d?c bi?t (không rollback du?c): `n nhom_code `n Các item_code / giá tr? `n TRINH_DO `n Ti?n si, Th?c si, Ð?i h?c, Cao d?ng, Trung c?p, So c?p `n HINH_THUC_DAO_TAO `n T?p trung, Tr?c tuy?n, K?t h?p, T? h?c `n KET_QUA_DAO_TAO `n Xu?t s?c, Gi?i, Khá, Ð?t, Không d?t, Ðang h?c `n LOAI_THIET_BI `n Máy tính d? bàn, Laptop, Máy tính b?ng, Máy in, Scanner, Server, Thi?t b? m?ng, Khác `n TINH_TRANG_THIET_BI `n T?t, Trung bình, H?ng, C?n b?o trì `n LOAI_KET_NOI `n LAN, WAN, BCANet, Wifi, 4G/5G, Viba, Cáp quang `n LOAI_HTTT `n Ph?n m?m nghi?p v?, CSDL, C?ng thông tin, H? th?ng email, ERP, Khác `n TRANG_THAI_HTTT `n Ðang v?n hành, T?m d?ng, Ng?ng s? d?ng, Ðang nâng c?p `n CAP_DO_ATTT `n 1, 2, 3, 4, 5 (theo TT17/2014/TT-BTTTT) `n LOAI_GIAI_PHAP_ATTT `n Firewall, IDS/IPS, Antivirus, Backup, Mã hóa, VPN, Quét l? h?ng, Khác `n LOAI_CAMERA `n IP Camera, PTZ, Dome, Bullet, Fisheye, Thermal `n TINH_TRANG_CAMERA `n Ho?t d?ng t?t, H?ng, C?n b?o trì, Chua k?t n?i `n LOAI_DU_AN `n Mua s?m ph?n c?ng, Phát tri?n ph?n m?m, Thuê d?ch v?, Nâng c?p h? th?ng, Ðào t?o `n TINH_TRANG_DU_AN `n K? ho?ch, Ðang tri?n khai, Hoàn thành, T?m d?ng, H?y `n LOAI_VAN_BAN `n Lu?t, Ngh? d?nh, Thông tu, Quy?t d?nh, Ch? th?, Công van, Ngh? quy?t `n LOAI_THONG_BAO `n KY_MO, KY_DONG, YEU_CAU_DUYET, YEU_CAU_TU_CHOI, HAN_NHAP_LIEU, HE_THONG `n TRANG_THAI_DAU_TU `n K? ho?ch, Ðang th?c hi?n, Hoàn thành, T?m d?ng `n Seed cho loai_don_vi và don_vi: `n Seed ~10 lo?i don v? (loai_don_vi): BCA, CAT, CAH, PC (Phòng C?nh sát), PA (Phòng An ninh), CAX, v.v. `n Seed danh sách ~100+ don v? CAND theo cây phân c?p t? file Excel cung c?p b?i H05. `n Seed 4 system role m?c d?nh: H05_ADMIN, DON_VI_ADMIN, DON_VI_USER, VIEWER. `n Seed toàn b? permission (1 permission / module / action): ~60 permission records. `n Seed gán permission m?c d?nh cho H05_ADMIN (t?t c?) và DON_VI_USER (read+write riêng module). `n 2.4.3 C?u hình EF Core AppDbContext (Oracle) `n // src/data-source.ts `n // AppDbContext.cs — EF Core `n `n public class AppDbContext : DbContext { `n // Oracle provider configured in Program.cs `n // via UseOracle(connectionString, o => o.UseOracleSQLCompatibility("12")) `n protected override void OnModelCreating(ModelBuilder mb) { `n mb.HasDefaultSchema("CAND_QLCNTT"); `n // Global Query Filter: soft-delete t? d?ng `n foreach (var e in mb.Model.GetEntityTypes()) `n if (typeof(ISoftDelete).IsAssignableFrom(e.ClrType)) `n mb.Entity(e.ClrType).HasQueryFilter(p => `n EF.Property<DateTime?>(p, "DeletedAt") == null); `n } `n } `n // Flyway commands: `n 3. Thi?t k? API (API Design) `n Ph?n này d?c t? toàn b? REST API c?a h? th?ng ThucLuc V2, bao g?m quy u?c thi?t k?, co ch? xác th?c/phân quy?n, chu?n hóa response, x? lý l?i, và danh sách d?y d? các endpoint theo t?ng module nghi?p v?. `n 3.1 T?ng quan & Quy u?c chung `n 3.1.1 Base URL & Versioning `n H? th?ng áp d?ng URL versioning theo ti?n t? /api/v{n}: `n Base URL: https://<host>/api/v1 `n T?t c? endpoint d?u d?t du?i ti?n t? /api/v1/. `n Nâng version API (v2, v3) ch? khi thay d?i không tuong thích ngu?c. `n Môi tru?ng: Host do H05 c?p phát, giao ti?p qua BCANet n?i b? (không Internet). `n 3.1.2 Quy u?c REST `n Quy u?c `n Giá tr? `n Giao th?c `n HTTPS (TLS 1.2+) `n Content-Type `n application/json; charset=utf-8 `n Datetime format `n ISO 8601 — yyyy-MM-ddTHH:mm:ssZ (UTC+7 luu offset) `n Pagination `n Query params: page (1-based), pageSize (m?c d?nh 20, t?i da 100) `n Sort `n Query params: sortBy=<field>, sortDir=asc|desc `n Soft-delete `n Không xóa v?t lý — dùng DELETE endpoint d? set deleted_at `n ID format `n S? nguyên (bigint/number) khóa chính auto-increment `n Tên field `n camelCase trong JSON ? snake_case trong DB `n 3.1.3 Phân quy?n RBAC `n M?i endpoint yêu c?u m?t permission code d?ng <module>:<action>. Controller s? d?ng attribute [RequirePermission("module:action")] (ASP.NET Core custom authorization policy). `n Action `n HTTP Method `n Ví d? permission `n read `n GET `n don_vi:read `n create `n POST `n don_vi:create `n update `n PUT / PATCH `n don_vi:update `n delete `n DELETE `n don_vi:delete `n export `n GET `n bao_cao:export `n approve `n POST `n ky_bao_cao:approve `n admin `n b?t k? `n system:admin `n 3.2 Xác th?c (Authentication) `n 3.2.1 JWT Bearer Token `n H? th?ng s? d?ng JWT (JSON Web Token) Bearer theo RFC 7519. Không dùng LDAP, SSO, hay OAuth2 (do yêu c?u b?o m?t n?i m?ng). `n Access Token: TTL 8 gi? (phù h?p ca làm vi?c). `n Refresh Token: TTL 7 ngày, luu trong HTTP-Only cookie. `n Token du?c ký b?ng thu?t toán RS256 (asymmetric key pair). `n Payload ch?a: userId, username, donViId, roles[], permissions[]. `n 3.2.2 Lu?ng dang nh?p `n POST /api/v1/auth/login Body: { username, password } Response 200: { "accessToken": "<JWT>", "expiresIn": 28800, "user": { "id": 1, "hoTen": "Nguy?n Van A", "donViId": 5, "roles": ["DON_VI_USER"] } } `n 3.2.3 Endpoints Auth `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n POST `n /auth/login `n Ðang nh?p, tr? v? access token `n Public `n POST `n /auth/refresh `n Làm m?i access token b?ng refresh token `n Public (cookie) `n POST `n /auth/logout `n Thu h?i refresh token `n Authenticated `n GET `n /auth/me `n L?y thông tin user hi?n t?i `n Authenticated `n PATCH `n /auth/change-password `n Ð?i m?t kh?u cá nhân `n Authenticated `n 3.3 Chu?n hóa Response `n 3.3.1 C?u trúc Response thành công `n // Ð?i tu?ng don { "success": true, "data": { ... }, "meta": null } // Danh sách phân trang { "success": true, "data": [ ... ], "meta": { "page": 1, "pageSize": 20, "totalItems": 350, "totalPages": 18 } } `n 3.3.2 C?u trúc Response l?i `n { "success": false, "error": { "code": "PERMISSION_DENIED", "message": "B?n không có quy?n th?c hi?n thao tác này.", "details": null } } `n 3.3.3 HTTP Status Code chu?n `n HTTP Code `n Ý nghia `n Khi nào dùng `n 200 OK `n Thành công `n GET / PUT / PATCH thành công `n 201 Created `n T?o m?i thành công `n POST t?o b?n ghi m?i `n 204 No Content `n Xóa thành công `n DELETE `n 400 Bad Request `n D? li?u d?u vào không h?p l? `n Validation error, thi?u field `n 401 Unauthorized `n Chua dang nh?p `n Token h?t h?n ho?c không có token `n 403 Forbidden `n Không có quy?n `n Token h?p l? nhung thi?u permission `n 404 Not Found `n Không tìm th?y `n ID không t?n t?i ho?c dã b? xóa `n 409 Conflict `n Trùng d? li?u `n UNIQUE constraint vi ph?m `n 422 Unprocessable Entity `n L?i nghi?p v? `n Vi ph?m business rule (BR-*) `n 500 Internal Server Error `n L?i server `n Exception không x? lý du?c `n 3.4 B?ng Error Code `n Error Code `n HTTP `n Mô t? `n INVALID_CREDENTIALS `n 401 `n Sai username ho?c password `n TOKEN_EXPIRED `n 401 `n Access token h?t h?n `n TOKEN_INVALID `n 401 `n Token không h?p l? ho?c b? gi? m?o `n PERMISSION_DENIED `n 403 `n Thi?u permission yêu c?u `n NOT_FOUND `n 404 `n B?n ghi không t?n t?i `n DUPLICATE_CODE `n 409 `n Mã dã t?n t?i trong h? th?ng `n KY_LOCKED `n 422 `n K? báo cáo dã khóa, không th? ch?nh s?a `n KY_NOT_OPEN `n 422 `n K? báo cáo chua m? ho?c dã dóng `n DON_VI_INACTIVE `n 422 `n Ðon v? dã b? vô hi?u hóa `n INVALID_PERIOD `n 422 `n K? báo cáo không h?p l? `n FILE_TOO_LARGE `n 400 `n File vu?t quá 25 MB `n FILE_TYPE_NOT_ALLOWED `n 400 `n Ð?nh d?ng file không du?c phép `n VALIDATION_ERROR `n 400 `n D? li?u d?u vào không h?p l? (chi ti?t trong details) `n 3.5 Danh sách API Endpoints `n 3.5.1 K? Báo cáo (/ky-bao-cao) `n Qu?n lý vòng d?i k? báo cáo (nam + quý). H05_ADMIN toàn quy?n; don v? ch? d?c. `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /ky-bao-cao `n Danh sách k? báo cáo (phân trang) `n ky_bao_cao:read `n GET `n /ky-bao-cao/:id `n Chi ti?t k? báo cáo `n ky_bao_cao:read `n GET `n /ky-bao-cao/hien-tai `n K? báo cáo dang m? hi?n t?i `n Authenticated `n POST `n /ky-bao-cao `n T?o k? báo cáo m?i `n ky_bao_cao:create `n PATCH `n /ky-bao-cao/:id/trang-thai `n Chuy?n tr?ng thái (mo/dong/khoa) `n ky_bao_cao:approve `n GET `n /ky-bao-cao/:id/tong-hop `n T?ng h?p ti?n d? n?p báo cáo toàn don v? `n ky_bao_cao:read `n GET `n /ky-bao-cao/:id/trang-thai-don-vi `n Tr?ng thái t?ng don v? trong k? `n ky_bao_cao:read `n 3.5.2 Ðon v? & Danh m?c (/don-vi, /danh-muc) `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /don-vi `n Danh sách don v? (cây phân c?p) `n don_vi:read `n GET `n /don-vi/:id `n Chi ti?t don v? `n don_vi:read `n POST `n /don-vi `n T?o don v? m?i `n don_vi:create `n PUT `n /don-vi/:id `n C?p nh?t don v? `n don_vi:update `n DELETE `n /don-vi/:id `n Soft-delete don v? `n don_vi:delete `n GET `n /don-vi/:id/users `n Danh sách user thu?c don v? `n don_vi:read `n GET `n /danh-muc `n Danh m?c (filter ?nhomCode=...) `n Authenticated `n POST `n /danh-muc `n Thêm m?c vào danh m?c `n system:admin `n PUT `n /danh-muc/:id `n C?p nh?t danh m?c `n system:admin `n DELETE `n /danh-muc/:id `n Vô hi?u hóa danh m?c `n system:admin `n 3.5.3 Ngu?i dùng & Phân quy?n (/users, /roles) `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /users `n Danh sách user (filter ?donViId=, ?role=) `n users:read `n GET `n /users/:id `n Chi ti?t user `n users:read `n POST `n /users `n T?o tài kho?n m?i `n users:create `n PUT `n /users/:id `n C?p nh?t thông tin user `n users:update `n PATCH `n /users/:id/kich-hoat `n Kích ho?t / vô hi?u hóa tài kho?n `n users:update `n POST `n /users/:id/reset-password `n Reset m?t kh?u (H05_ADMIN) `n users:admin `n PUT `n /users/:id/roles `n Gán/thu h?i role cho user `n users:admin `n GET `n /roles `n Danh sách role `n roles:read `n POST `n /roles `n T?o role tùy ch?nh `n roles:create `n PUT `n /roles/:id/permissions `n C?p nh?t permissions c?a role `n roles:admin `n GET `n /permissions `n Toàn b? permission trong h? th?ng `n system:admin `n 3.5.4 Các Module Nghi?p v? — M?u chung `n T?t c? 17 module nghi?p v? d?u áp d?ng cùng m?t m?u CRUD. Thay {module} b?ng tên module tuong ?ng (b?ng du?i). `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /{module} `n Danh sách b?n ghi c?a don v? trong k? (?kyId=, ?donViId=) `n {module}:read `n GET `n /{module}/:id `n Chi ti?t b?n ghi `n {module}:read `n POST `n /{module} `n T?o / luu b?n ghi m?i `n {module}:create `n PUT `n /{module}/:id `n C?p nh?t toàn b? b?n ghi `n {module}:update `n PATCH `n /{module}/:id `n C?p nh?t m?t ph?n b?n ghi `n {module}:update `n DELETE `n /{module}/:id `n Soft-delete b?n ghi `n {module}:delete `n POST `n /{module}/:id/nop `n N?p b?n ghi (chuy?n sang tr?ng thái ÐÃ_N?P) `n {module}:update `n POST `n /{module}/:id/phe-duyet `n Phê duy?t b?n ghi (H05_ADMIN / DON_VI_ADMIN) `n {module}:approve `n POST `n /{module}/:id/yeu-cau-bo-sung `n Yêu c?u b? sung (tr? v? don v? ch?nh s?a) `n {module}:approve `n GET `n /{module}/export `n Xu?t Excel / CSV báo cáo `n {module}:export `n B?ng ánh x? module URL ? b?ng DB ? permission prefix: `n URL {module} `n B?ng DB chính `n Permission prefix `n Nhóm SRS `n van-ban-qppl `n van_ban_qppl `n van_ban_qppl `n §6.1 `n nhan-luc-cntt `n nhan_luc_cntt `n nhan_luc_cntt `n §6.2 `n dao-tao-boi-duong `n dao_tao_boi_duong `n dao_tao_boi_duong `n §6.3 `n dao-tao-hoc-vien `n dao_tao_hoc_vien `n dao_tao_hoc_vien `n §6.4 `n nang-luc-so `n nang_luc_so `n nang_luc_so `n §6.5 `n dao-tao-nuoc-ngoai `n dao_tao_nuoc_ngoai `n dao_tao_nuoc_ngoai `n §6.6 `n thiet-bi-cntt `n thiet_bi_cntt `n thiet_bi_cntt `n §6.7 `n ha-tang-mang `n ha_tang_mang `n ha_tang_mang `n §6.8 `n he-thong-thong-tin `n he_thong_thong_tin `n he_thong_thong_tin `n §6.9 `n giam-sat-soc `n giam_sat_soc `n giam_sat_soc `n §6.10 `n giam-sat-noc `n giam_sat_noc `n giam_sat_noc `n §6.10 `n attt-httt-van-hanh `n attt_httt_van_hanh `n attt_httt `n §6.11 `n attt-httt-dau-tu `n attt_httt_dau_tu `n attt_httt `n §6.11 `n giai-phap-attt `n giai_phap_attt `n attt_httt `n §6.11 `n camera-quan-ly `n camera_quan_ly `n camera `n §6.12 `n camera-thuc-trang `n camera_thuc_trang `n camera `n §6.12 `n du-an-cntt `n du_an_cntt `n du_an_cntt `n §6.13 `n 3.5.5 T?p dính kèm (/files) `n T?p dính kèm du?c luu trên MinIO (S3-compatible). Upload dùng multipart/form-data; download tr? v? presigned URL. `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n POST `n /files/upload `n Upload file — multipart/form-data (max 25 MB) `n Authenticated `n GET `n /files/:id/download `n L?y presigned URL download (TTL 15 phút) `n Authenticated `n DELETE `n /files/:id `n Xóa t?p (soft-delete metadata, xóa MinIO object) `n Authenticated `n GET `n /files `n Danh sách file dính kèm theo entity (?entityType=, ?entityId=) `n Authenticated `n Lo?i file cho phép: .pdf, .doc, .docx, .xls, .xlsx, .png, .jpg, .zip — t?i da 25 MB/file. `n 3.5.6 Thông báo (/thong-bao) `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /thong-bao `n Danh sách thông báo c?a user hi?n t?i `n Authenticated `n PATCH `n /thong-bao/:id/da-doc `n Ðánh d?u dã d?c `n Authenticated `n PATCH `n /thong-bao/da-doc-tat `n Ðánh d?u t?t c? dã d?c `n Authenticated `n DELETE `n /thong-bao/:id `n Xóa thông báo `n Authenticated `n 3.5.7 Nh?t ký h? th?ng (/system-log) `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /system-log `n Tra c?u nh?t ký (filter ?userId=, ?action=, ?from=, ?to=) `n system:admin `n GET `n /system-log/export `n Xu?t nh?t ký ra Excel `n system:admin `n 3.5.8 Báo cáo t?ng h?p (/bao-cao) `n Endpoint t?ng h?p da module, ph?c v? màn hình th?ng kê và xu?t báo cáo H05. `n Method `n Endpoint `n Mô t? `n Permission yêu c?u `n GET `n /bao-cao/tong-hop `n T?ng h?p t?t c? module cho k? (?kyId=) `n bao_cao:read `n GET `n /bao-cao/tien-do `n Ti?n d? n?p báo cáo toàn qu?c (?kyId=) `n bao_cao:read `n GET `n /bao-cao/don-vi/:id `n T?ng h?p báo cáo theo don v? (?kyId=) `n bao_cao:read `n GET `n /bao-cao/export/excel `n Xu?t báo cáo t?ng h?p toàn b? module ra Excel `n bao_cao:export `n GET `n /bao-cao/export/module/:module `n Xu?t báo cáo riêng t?ng module ra Excel `n bao_cao:export `n 3.6 Ki?n trúc Controller & Middleware (ASP.NET Core 8) `n 3.6.1 C?u trúc thu m?c Backend `n src/ +-- Controllers/ # API Controllers (1 controller / module) +-- Services/ # Business logic, g?i Repository +-- Repositories/ # Data access (EF Core + Oracle) +-- Entities/ # EF Core entity models (ánh x? DB) +-- DTOs/ # Request / Response DTOs +-- Middleware/ ¦ +-- JwtMiddleware.cs # Validate & parse JWT ¦ +-- PermissionHandler.cs # RBAC permission check ¦ +-- AuditLogMiddleware.cs # T? d?ng ghi system_log +-- Filters/ ¦ +-- ValidationFilter.cs # Tr? 400 khi ModelState invalid ¦ +-- GlobalExceptionFilter.cs # B?t Exception ? 500 +-- Program.cs # DI container, middleware pipeline `n 3.6.2 Ví d? Controller m?u `n [ApiController] [Route("api/v1/[controller]")] [Authorize] // Yêu c?u JWT h?p l? public class NhanLucCnttController : ControllerBase { [HttpGet] [RequirePermission("nhan_luc_cntt:read")] public async Task<IActionResult> GetList([FromQuery] QueryParams q) => Ok(await _service.GetPagedAsync(q)); [HttpPost] [RequirePermission("nhan_luc_cntt:create")] public async Task<IActionResult> Create([FromBody] NhanLucCnttDto dto) => CreatedAtAction(nameof(GetById), new { id = result.Id }, result); [HttpPost("{id}/nop")] [RequirePermission("nhan_luc_cntt:update")] public async Task<IActionResult> Nop(long id) => Ok(await _service.NopAsync(id, CurrentUserId)); } `n 3.6.3 Audit Log t? d?ng `n Middleware AuditLogMiddleware t? d?ng ghi nh?t ký m?i request thay d?i d? li?u (POST/PUT/PATCH/DELETE) vào b?ng system_log: `n Field `n Ngu?n `n user_id `n JWT claims `n don_vi_id `n JWT claims `n action `n HTTP method + route template `n entity_type `n Controller name (prefix) `n entity_id `n Route param :id `n ip_address `n X-Forwarded-For / RemoteIpAddress `n created_at `n UTC server time `n 3.7 Tích h?p MinIO (Object Storage) `n 3.7.1 C?u hình `n // appsettings.json "MinIO": { "Endpoint": "minio.h05.local:9000", "AccessKey": "<env>", "SecretKey": "<env>", "BucketName": "thuc-luc-v2", "UseSSL": true } `n 3.7.2 Lu?ng Upload / Download `n Upload: Client ? POST /files/upload ? Backend stream file ? MinIO.PutObject ? luu metadata vào file_dinh_kem. `n Download: Client ? GET /files/:id/download ? Backend xác th?c quy?n ? MinIO.PresignedGetObject (TTL 15 phút) ? tr? URL cho client. `n Object key: {donViId}/{kyCode}/{entityType}/{uuid}.{ext} — tránh va ch?m, d? phân quy?n bucket policy. `n 3.7.3 Xu?t PDF — Gotenberg `n Gotenberg là Docker container chuyên render HTML/Markdown ? PDF. Backend g?i Gotenberg API HTTP, không c?n headless browser trên app server. `n // C?u hình appsettings.json `n "Gotenberg": { `n "BaseUrl": "http://gotenberg:3000" `n } `n // Lu?ng xu?t PDF báo cáo: `n // 1. Backend render Razor view ? HTML string `n // 2. POST http://gotenberg:3000/forms/chromium/convert/html `n // multipart: files["index.html"] = htmlContent `n // 3. Gotenberg tr? v? PDF bytes `n // 4. Backend stream PDF v? client ho?c luu MinIO `n 3.8 K?t n?i Oracle Database (EF Core + ODP.NET) `n 3.8.1 Connection string `n // appsettings.json "ConnectionStrings": { "OracleDb": "User Id=cand_app;Password=<env>;Data Source=//dbserver:1521/CAND" } `n 3.8.2 EF Core Oracle provider `n // Program.cs builder.Services.AddDbContext<AppDbContext>(opt => opt.UseOracle(connStr, o => o.UseOracleSQLCompatibility("12"))); // Luu ý: // - Schema m?c d?nh: CAND_QLCNTT // - Sequence cho PK: Oracle dùng SEQUENCE thay IDENTITY // - Datetime: dùng Oracle TIMESTAMP WITH TIME ZONE // - Soft-delete: Global Query Filter WHERE deleted_at IS NULL `n 3.8.3 Migration strategy `n Dùng Flyway v?i provider Oracle.EntityFrameworkCore (ODP.NET). `n Không dùng synchronize:true — m?i thay d?i schema qua migration file du?c review tru?c khi deploy. `n Script migration du?c export ra .sql, DBA H05 review và ch?y th? công trên môi tru?ng production. `n Rollback: m?i migration có method Down() tuong ?ng. `n 3.9 B?o m?t API `n Bi?n pháp `n Cách th?c hi?n `n HTTPS only `n Nginx reverse proxy, HTTP ? redirect 301 ? HTTPS `n JWT RS256 `n Private key ký trên server, public key verify — không dùng HS256 shared secret `n Rate limiting `n ASP.NET Core RateLimiter middleware — 60 req/min/IP cho endpoint login `n Input validation `n FluentValidation cho t?t c? DTO — reject s?m tru?c khi vào service `n SQL injection `n EF Core parameterized query — không dùng raw SQL tr? stored procedure dã ki?m tra `n CORS `n Ch? cho phép origin n?i m?ng BCANet (whitelist IP range) `n Sensitive data masking `n Password hash bcrypt (cost=12); không log password, token trong system_log `n File type validation `n Ki?m tra MIME type + magic bytes — không tin vào Content-Type c?a client `n 4. Thi?t k? Giao di?n (Frontend Design) `n Ph?n này mô t? ki?n trúc giao di?n ngu?i dùng c?a h? th?ng ThucLuc V2, bao g?m c?u trúc Angular SPA, qu?n lý state, routing, danh sách màn hình, và các nguyên t?c UX/UI áp d?ng. `n 4.1 T?ng quan ki?n trúc Frontend `n 4.1.1 Technology Stack `n Thành ph?n `n Công ngh? `n Phiên b?n `n M?c dích `n Framework `n Angular `n 17 (Standalone API) `n SPA framework chính `n Language `n TypeScript `n 5.x `n Type safety, IDE support `n UI Component `n Angular Material `n 17.x `n Component library chu?n Material Design `n State Management `n NgRx `n 17.x `n Qu?n lý global state (Store + Effects) `n HTTP Client `n Angular HttpClient `n built-in `n Giao ti?p v?i Backend API `n Form `n Reactive Forms `n built-in `n Validation ph?c t?p, dynamic form `n Charts `n ECharts (ngx-echarts) `n 5.x `n Bi?u d? th?ng kê `n Export Excel `n ExcelJS `n 4.x `n Xu?t báo cáo client-side `n Build tool `n Angular CLI + esbuild `n 17.x `n Build, serve, test `n Web server `n Nginx `n 1.25 `n Serve static files, reverse proxy `n 4.1.2 Nguyên t?c thi?t k? UI `n Giao di?n don gi?n, phù h?p nghi?p v? hành chính — không dùng animation ph?c t?p. `n Responsive: t?i thi?u h? tr? màn hình 1280×720 (desktop/laptop công v?). `n Ti?ng Vi?t toàn b? — không dùng label ti?ng Anh trên giao di?n ngu?i dùng. `n Phân quy?n UI: ?n/disable button/menu khi user không có permission tuong ?ng. `n Loading state rõ ràng: spinner khi g?i API, skeleton khi load d? li?u d?u tiên. `n Xác nh?n tru?c các thao tác không kh? ngh?ch (xóa, n?p, phê duy?t, khóa k?). `n 4.2 C?u trúc thu m?c Frontend `n src/ +-- app/ ¦ +-- core/ # Singleton services, guards, interceptors ¦ ¦ +-- auth/ # AuthService, JwtInterceptor, AuthGuard ¦ ¦ +-- services/ # ApiService base, NotificationService ¦ ¦ +-- store/ # NgRx root state (auth, ui) ¦ +-- shared/ # Components/pipes dùng l?i nhi?u noi ¦ ¦ +-- components/ # PageHeader, DataTable, ConfirmDialog, ... ¦ ¦ +-- pipes/ # DateVN, FileSize, TrangThai pipes ¦ ¦ +-- directives/ # HasPermission directive ¦ +-- layout/ # Shell layout ¦ ¦ +-- sidebar/ # Menu d?c bên trái ¦ ¦ +-- header/ # Top bar (user info, thông báo) ¦ ¦ +-- main-layout/ # Router outlet chính ¦ +-- features/ # Lazy-loaded feature modules ¦ +-- auth/ # Màn hình dang nh?p ¦ +-- dashboard/ # Trang t?ng quan ¦ +-- ky-bao-cao/ # Qu?n lý k? báo cáo ¦ +-- don-vi/ # Qu?n lý don v? ¦ +-- users/ # Qu?n lý ngu?i dùng ¦ +-- nghiep-vu/ # 17 module nghi?p v? (lazy) ¦ +-- bao-cao/ # Báo cáo t?ng h?p & xu?t Excel ¦ +-- admin/ # C?u hình h? th?ng, nh?t ký +-- assets/ ¦ +-- images/ ¦ +-- i18n/ # File d?ch (vi.json) +-- environments/ +-- environment.ts # Dev: API_URL=http://localhost:5000 +-- environment.prod.ts # Prod: API_URL=https://thuc-luc.h05.local `n 4.3 Qu?n lý State (NgRx) `n 4.3.1 Store structure `n Feature Store `n State ch?a `n Dùng ? `n auth `n currentUser, token, permissions[] `n Toàn h? th?ng — guard, directive `n ui `n isLoading, sidebarCollapsed, notifications[] `n Layout, thông báo `n ky-bao-cao `n danhSachKy[], kyHienTai, trangThaiDonVi[] `n Dashboard, nh?p li?u `n don-vi `n tree[], selectedDonVi `n Filter toàn c?c `n nghiep-vu `n currentList[], currentRecord, pagination `n Màn hình nh?p li?u `n bao-cao `n tongHop, tienDo, chartData `n Màn hình báo cáo `n 4.3.2 Auth flow v?i NgRx Effects `n // Lu?ng dang nh?p Component dispatch LoginAction ? AuthEffects.login$ g?i API POST /auth/login ? success: dispatch LoginSuccessAction ? luu token, redirect dashboard ? error: dispatch LoginFailureAction ? hi?n thông báo l?i // Auto-refresh token JwtInterceptor b?t 401 response ? dispatch RefreshTokenAction ? success: retry request g?c ? failure: dispatch LogoutAction ? redirect /login `n 4.4 Routing & Navigation `n 4.4.1 Cây route chính `n /login ? AuthModule (public) / ? MainLayout (AuthGuard) /dashboard ? DashboardModule /ky-bao-cao ? KyBaoCaoModule /:id ? Chi ti?t k? /:id/tien-do ? Ti?n d? n?p /don-vi ? DonViModule /users ? UsersModule [perm: users:read] /nghiep-vu/:module ? NghiepVuModule (lazy, 1 route cho 17 module) / ? Danh sách b?n ghi /them-moi ? Form t?o m?i /:id ? Chi ti?t / ch?nh s?a /bao-cao ? BaoCaoModule /tong-hop ? Báo cáo t?ng h?p /tien-do ? Bi?u d? ti?n d? /xuat-excel ? Xu?t Excel /admin ? AdminModule [perm: system:admin] /danh-muc ? Qu?n lý danh m?c /roles ? Qu?n lý roles /system-log ? Nh?t ký h? th?ng /** ? 404 NotFound `n 4.4.2 Guards `n Guard `n Ði?u ki?n `n Redirect n?u không th?a `n AuthGuard `n Token JWT còn hi?u l?c `n /login `n PermissionGuard `n User có permission yêu c?u c?a route `n /403 `n KyOpenGuard `n K? báo cáo dang trong tr?ng thái M? `n /ky-bao-cao (thông báo) `n UnsavedGuard `n Form chua luu — confirm tru?c khi r?i `n ? l?i trang `n 4.5 Danh sách màn hình `n Mã MH `n Tên màn hình `n Route `n Quy?n t?i thi?u `n MH-01 `n Ðang nh?p `n /login `n Public `n MH-02 `n Trang t?ng quan (Dashboard) `n /dashboard `n Authenticated `n MH-03 `n Danh sách k? báo cáo `n /ky-bao-cao `n ky_bao_cao:read `n MH-04 `n Chi ti?t k? — ti?n d? don v? `n /ky-bao-cao/:id/tien-do `n ky_bao_cao:read `n MH-05 `n Danh sách don v? `n /don-vi `n don_vi:read `n MH-06 `n Thêm / s?a don v? `n /don-vi/:id `n don_vi:create `n MH-07 `n Danh sách ngu?i dùng `n /users `n users:read `n MH-08 `n Thêm / s?a ngu?i dùng `n /users/:id `n users:create `n MH-09 `n Qu?n lý roles & permissions `n /admin/roles `n roles:admin `n MH-10 `n Nh?p li?u — Van b?n QPPL `n /nghiep-vu/van-ban-qppl `n van_ban_qppl:read `n MH-11 `n Nh?p li?u — Nhân l?c CNTT `n /nghiep-vu/nhan-luc-cntt `n nhan_luc_cntt:read `n MH-12 `n Nh?p li?u — Ðào t?o b?i du?ng `n /nghiep-vu/dao-tao-boi-duong `n dao_tao_boi_duong:read `n MH-13 `n Nh?p li?u — Ðào t?o h?c viên `n /nghiep-vu/dao-tao-hoc-vien `n dao_tao_hoc_vien:read `n MH-14 `n Nh?p li?u — Nang l?c s? `n /nghiep-vu/nang-luc-so `n nang_luc_so:read `n MH-15 `n Nh?p li?u — ÐT nu?c ngoài `n /nghiep-vu/dao-tao-nuoc-ngoai `n dao_tao_nuoc_ngoai:read `n MH-16 `n Nh?p li?u — Thi?t b? CNTT `n /nghiep-vu/thiet-bi-cntt `n thiet_bi_cntt:read `n MH-17 `n Nh?p li?u — H? t?ng m?ng `n /nghiep-vu/ha-tang-mang `n ha_tang_mang:read `n MH-18 `n Nh?p li?u — H? th?ng TT `n /nghiep-vu/he-thong-thong-tin `n he_thong_thong_tin:read `n MH-19 `n Nh?p li?u — Giám sát SOC/NOC `n /nghiep-vu/giam-sat-soc `n giam_sat_soc:read `n MH-20 `n Nh?p li?u — ATTT HTTT `n /nghiep-vu/attt-httt-van-hanh `n attt_httt:read `n MH-21 `n Nh?p li?u — Gi?i pháp ATTT `n /nghiep-vu/giai-phap-attt `n attt_httt:read `n MH-22 `n Nh?p li?u — Camera `n /nghiep-vu/camera-quan-ly `n camera:read `n MH-23 `n Nh?p li?u — D? án CNTT `n /nghiep-vu/du-an-cntt `n du_an_cntt:read `n MH-24 `n Phê duy?t & Yêu c?u b? sung `n /nghiep-vu/:module/:id `n {module}:approve `n MH-25 `n Báo cáo t?ng h?p `n /bao-cao/tong-hop `n bao_cao:read `n MH-26 `n Bi?u d? ti?n d? n?p `n /bao-cao/tien-do `n bao_cao:read `n MH-27 `n Xu?t Excel báo cáo `n /bao-cao/xuat-excel `n bao_cao:export `n MH-28 `n Qu?n lý danh m?c `n /admin/danh-muc `n system:admin `n MH-29 `n Nh?t ký h? th?ng `n /admin/system-log `n system:admin `n MH-30 `n Thông báo (panel) `n overlay trên header `n Authenticated `n 4.6 Mô t? màn hình chính `n 4.6.1 MH-01 — Ðang nh?p `n Thành ph?n `n Mô t? `n Logo + tên h? th?ng `n B? Công An — C?c CNTT — ThucLuc V2 `n Form dang nh?p `n Input: Tên dang nh?p, M?t kh?u (?n). Button: Ðang nh?p `n Thông báo l?i `n Hi?n th? inline khi sai credential ho?c tài kho?n b? khóa `n X? lý loading `n Button disable + spinner khi dang g?i API `n B?o m?t `n Không luu password trong localStorage. Rate limit hi?n th? d?m ngu?c `n 4.6.2 MH-02 — Trang t?ng quan (Dashboard) `n Widget `n D? li?u hi?n th? `n Ngu?n API `n K? báo cáo hi?n t?i `n Tên k?, tr?ng thái, deadline, s? ngày còn l?i `n GET /ky-bao-cao/hien-tai `n Ti?n d? n?p `n Donut chart: % don v? dã n?p / chua n?p / quá h?n `n GET /ky-bao-cao/:id/tong-hop `n Th?ng kê nhanh `n T?ng don v?, t?ng user, t?ng b?n ghi k? này `n GET /bao-cao/tong-hop `n Bi?u d? c?t `n So sánh s? b?n ghi theo module trong k? `n GET /bao-cao/tong-hop `n Danh sách don v? `n Top don v? chua n?p báo cáo (H05_ADMIN only) `n GET /ky-bao-cao/:id/trang-thai-don-vi `n Thông báo g?n dây `n 5 thông báo m?i nh?t chua d?c `n GET /thong-bao `n 4.6.3 MH-03 — Qu?n lý k? báo cáo (H05_ADMIN) `n Thành ph?n `n Mô t? `n B?ng danh sách `n C?t: K? code, Nam, Quý, Tr?ng thái, Deadline, Thao tác. Phân trang 20/trang `n Badge tr?ng thái `n NHAP (xám) / MO (xanh) / DONG (cam) / KHOA (d?) `n T?o k? m?i `n Modal: nh?p Nam, Quý, Deadline. T? sinh ky_code = {YYYY}Q{Q} `n Chuy?n tr?ng thái `n Button v?i xác nh?n: MO ? DONG ? KHOA. Không th? d?o ngu?c KHOA `n Xem ti?n d? `n Link ? MH-04 ti?n d? don v? `n 4.6.4 MH-10 d?n MH-23 — Nh?p li?u Module Nghi?p v? (m?u chung) `n T?t c? 17 module nghi?p v? dùng chung 1 template Angular component, c?u hình qua ModuleConfig: `n Vùng `n N?i dung `n Header `n Tên module, tên don v?, k? báo cáo, tr?ng thái (badge) `n Toolbar `n Buttons: [+ Thêm m?i] [Xu?t Excel] [N?p báo cáo] — ?n/hi?n theo permission và tr?ng thái k? `n B?ng d? li?u `n ag-Grid / Angular Material Table. C?t d?ng theo ModuleConfig. Inline edit cho các tru?ng don gi?n `n Form chi ti?t `n Reactive Form. Validation rules t? ModuleConfig. H? tr? file dính kèm `n Tr?ng thái b?n ghi `n Badge: NHAP / DA_NOP / DA_DUYET / YEU_CAU_BO_SUNG `n L?ch s? `n Timeline các l?n ch?nh s?a, n?p, phê duy?t `n Yêu c?u b? sung `n Hi?n th? comment c?a H05 khi tr? v? (tr?ng thái YEU_CAU_BO_SUNG) `n 4.6.5 MH-25 — Báo cáo t?ng h?p `n Thành ph?n `n Mô t? `n B? l?c `n Dropdown: K? báo cáo, Lo?i don v?, Ðon v? c? th?. Button: [Áp d?ng] `n Tab modules `n M?i tab = 1 module nghi?p v?. Hi?n th? b?ng d? li?u t?ng h?p toàn qu?c `n Bi?u d? `n Bar chart so sánh gi?a các don v? theo ch? tiêu chính c?a module `n Xu?t Excel `n Button [Xu?t Excel] — g?i GET /bao-cao/export/excel ? t?i file .xlsx `n Phân quy?n `n DON_VI_USER ch? th?y d? li?u don v? mình. H05_ADMIN th?y t?t c? `n 4.6.6 MH-29 — Nh?t ký h? th?ng (system:admin) `n Thành ph?n `n Mô t? `n B? l?c `n User, Ðon v?, Action (POST/PUT/DELETE), Module, T? ngày - Ð?n ngày `n B?ng log `n C?t: Th?i gian, User, Ðon v?, Hành d?ng, Module, Record ID, IP Address `n Pagination `n Server-side paging 50 dòng/trang `n Xu?t Excel `n GET /system-log/export — t?i da 10.000 dòng/l?n `n Xem chi ti?t `n Modal: JSON diff before_data ? after_data `n 4.7 Shared Components `n Component `n Input / Props `n Dùng ? `n PageHeaderComponent `n title, breadcrumbs[], actions[] `n T?t c? trang `n DataTableComponent `n columns[], data[], pagination, loading `n M?i danh sách `n ConfirmDialogComponent `n message, confirmLabel, isDangerous `n Xóa, n?p, phê duy?t `n StatusBadgeComponent `n status: TrangThai `n M?i noi có tr?ng thái `n FileUploadComponent `n entityType, entityId, accept, maxSize `n Form chi ti?t `n HasPermissionDirective `n *hasPermission="'module:action'" `n ?n/hi?n button `n LoadingOverlayComponent `n isLoading: Observable<boolean> `n Layout chính `n EmptyStateComponent `n icon, message, actionLabel `n Danh sách r?ng `n FilterBarComponent `n filters: FilterConfig[], (onChange) `n Màn hình danh sách `n NotificationPanelComponent `n notifications$: Observable `n Header `n 4.8 HTTP Interceptors & Error Handling `n Interceptor `n Ch?c nang `n JwtInterceptor `n T? d?ng dính kèm Authorization: Bearer <token> vào m?i request có requireAuth=true `n RefreshInterceptor `n B?t 401 ? g?i POST /auth/refresh ? retry request g?c. N?u refresh th?t b?i ? logout `n LoadingInterceptor `n Tang/gi?m loading counter ? dispatch UI action isLoading `n ErrorInterceptor `n Map error response ? hi?n toast thông báo (snackbar). Log l?i 5xx v? console `n BaseUrlInterceptor `n T? d?ng prepend environment.apiUrl vào các relative URL `n 4.9 Chi?n lu?c Hi?u su?t & Build `n K? thu?t `n Cách th?c hi?n `n L?i ích `n Lazy loading modules `n M?i feature module lazy load qua loadComponent() `n Gi?m initial bundle ~60% `n OnPush Change Detection `n Dùng ChangeDetectionStrategy.OnPush cho t?t c? component `n Gi?m re-render `n Virtual scrolling `n cdk-virtual-scroll-viewport cho danh sách dài > 200 dòng `n Smooth scroll `n HTTP caching `n Cache GET /danh-muc và /don-vi vào NgRx store TTL 10 phút `n Gi?m API call `n Production build `n ng build --configuration=production ? esbuild tree-shaking `n Bundle t?i uu `n Nginx gzip `n gzip on; gzip_types text/html application/javascript; `n Gi?m bandwidth `n Preloading strategy `n PreloadAllModules cho route thu?ng dùng `n UX mu?t hon `n 5. Thi?t k? B?o m?t (Security Design) `n Ph?n này mô t? toàn di?n các bi?n pháp b?o m?t c?a h? th?ng ThucLuc V2 trong b?i c?nh tri?n khai n?i m?ng BCANet — không ti?p c?n Internet công c?ng. `n 5.1 Mô hình b?o m?t t?ng th? `n H? th?ng áp d?ng mô hình Defense-in-Depth (phòng th? nhi?u l?p): `n L?p `n Bi?n pháp `n Công c?/Chu?n `n L1 — M?ng `n Gi?i h?n k?t n?i qua BCANet n?i b?, firewall H05 `n Firewall c?p m?ng, VLAN `n L2 — Truy?n thông `n Mã hóa TLS 1.2+ toàn b? traffic HTTPS `n Nginx TLS, cert n?i b? `n L3 — Xác th?c `n JWT RS256, bcrypt password, rate limiting login `n ASP.NET Core Identity `n L4 — Phân quy?n `n RBAC d?ng: permission per module per action `n Custom AuthorizationPolicy `n L5 — ?ng d?ng `n Input validation, parameterized query, XSS/CSRF protection `n FluentValidation, EF Core `n L6 — D? li?u `n Soft-delete, audit log, backup mã hóa `n Oracle TDE, AES-256 `n L7 — Giám sát `n Ghi nh?t ký m?i thao tác, c?nh báo b?t thu?ng `n system_log, Prometheus `n 5.2 Xác th?c (Authentication) `n 5.2.1 Password Policy `n Quy t?c `n Giá tr? `n Ð? dài t?i thi?u `n 8 ký t? `n Ð? ph?c t?p `n B?t bu?c: ch? hoa, ch? thu?ng, s?, ký t? d?c bi?t `n Th?i h?n m?t kh?u `n 90 ngày — h? th?ng nh?c d?i khi còn 7 ngày `n L?ch s? m?t kh?u `n Không dùng l?i 5 m?t kh?u g?n nh?t `n Khóa tài kho?n `n 5 l?n dang nh?p sai ? khóa 15 phút `n Thu?t toán hash `n bcrypt, cost factor = 12 `n Reset m?t kh?u `n Ch? H05_ADMIN th?c hi?n — không qua email (không có SMTP) `n 5.2.2 JWT Token Security `n Thu?c tính `n Giá tr? / Cách th?c hi?n `n Thu?t toán ký `n RS256 — asymmetric (private key ký, public key verify) `n Access Token TTL `n 8 gi? (ca làm vi?c). Không luu trong localStorage — Memory only `n Refresh Token TTL `n 7 ngày. Luu trong HTTP-Only Secure cookie `n Token revocation `n Refresh token luu DB — logout xóa record ? không th? refresh `n Payload `n sub, username, donViId, roles[], permissions[], iat, exp `n Key rotation `n Private key thay m?i 90 ngày — refresh token force re-login `n CSRF protection `n SameSite=Strict cookie + custom header X-Requested-With `n 5.3 Phân quy?n RBAC `n 5.3.1 Ma tr?n phân quy?n m?c d?nh `n Permission `n H05_ADMIN `n DON_VI_ADMIN `n DON_VI_USER `n VIEWER `n ky_bao_cao:create/approve `n ? `n ? `n ? `n ? `n don_vi:create/update/delete `n ? `n ? `n ? `n ? `n users:create/admin `n ? `n ? (don v? mình) `n ? `n ? `n {module}:read `n ? `n ? `n ? `n ? `n {module}:create/update `n ? `n ? `n ? `n ? `n {module}:delete `n ? `n ? `n ? `n ? `n {module}:approve `n ? `n ? `n ? `n ? `n {module}:export `n ? `n ? `n ? `n ? `n bao_cao:read (t?t c? don v?) `n ? `n ? `n ? `n ? `n system:admin `n ? `n ? `n ? `n ? `n 5.3.2 Data scoping theo don v? `n DON_VI_USER và DON_VI_ADMIN ch? xem/ch?nh s?a d? li?u c?a don v? mình (don_vi_id t? JWT). `n Global Query Filter EF Core t? d?ng WHERE don_vi_id = @currentDonViId cho m?i query. `n H05_ADMIN không b? ràng bu?c — có th? xem toàn b? d? li?u. `n API validate: n?u body.donViId ? JWT.donViId ? 403 Forbidden (tr? H05_ADMIN). `n 5.4 B?o m?t t?ng ?ng d?ng `n 5.4.1 Phòng ch?ng OWASP Top 10 `n R?i ro `n Bi?n pháp `n A01 Broken Access Control `n RBAC enforcement m?i endpoint + data scoping theo don_vi_id `n A02 Cryptographic Failures `n TLS 1.2+, bcrypt hash, JWT RS256, Oracle TDE `n A03 Injection `n EF Core parameterized query — không raw SQL. FluentValidation input `n A04 Insecure Design `n Security review trong sprint, threat modeling cho flow quan tr?ng `n A05 Security Misconfiguration `n Production appsettings không commit, secrets qua environment variable `n A06 Vulnerable Components `n Dependabot alerts, c?p nh?t NuGet/npm d?nh k? `n A07 Auth Failures `n Rate limiting login, bcrypt cost=12, account lockout `n A08 Software Integrity Failures `n Build t? source n?i b?, không dùng CDN public `n A09 Logging Failures `n Ghi system_log m?i thao tác thay d?i, log không xóa du?c `n A10 SSRF `n H? th?ng không call external URL — không applicable `n 5.4.2 Input Validation `n T?t c? DTO dùng FluentValidation: ki?m tra type, length, range, format, required. `n File upload: ki?m tra MIME type + magic bytes (4 byte d?u) — không tin Content-Type header. `n SQL: EF Core parameterized query — không interpolate user input vào query string. `n XSS: Angular m?c d?nh encode HTML output — không dùng [innerHTML] v?i d? li?u t? server. `n CSRF: SameSite=Strict cookie + X-Requested-With header validation. `n 5.5 B?o m?t d? li?u `n Lo?i d? li?u `n Bi?n pháp b?o v? `n M?t kh?u `n bcrypt hash — không luu plaintext ho?c reversible encryption `n D? li?u nh?y c?m DB `n Oracle TDE (Transparent Data Encryption) mã hóa tablespace `n File dính kèm `n MinIO luu trên storage server, presigned URL TTL 15 phút `n Backup `n Backup file mã hóa AES-256 tru?c khi ghi ra Backup Server `n Log `n system_log ch? INSERT — không UPDATE/DELETE (append-only) `n Token `n Không log JWT/refresh token trong b?t k? file log nào `n K?t n?i DB `n Credentials qua environment variable — không hardcode trong code `n 5.6 B?o m?t h? t?ng `n 5.6.1 Nginx Security Headers `n # nginx.conf add_header Strict-Transport-Security "max-age=31536000" always; add_header X-Frame-Options "DENY" always; add_header X-Content-Type-Options "nosniff" always; add_header X-XSS-Protection "1; mode=block" always; add_header Referrer-Policy "no-referrer" always; add_header Content-Security-Policy "default-src 'self'" always; # ?n version Nginx server_tokens off; `n 5.6.2 Network Security `n H? th?ng ch? l?ng nghe trên interface BCANet n?i b? — không bind 0.0.0.0. `n Firewall H05: ch? m? port 443 (HTTPS) t? d?i IP BCANet du?c phép. `n Database server Oracle: ch? nh?n k?t n?i t? IP Application Server (whitelist). `n MinIO: không expose public — ch? Application Server có th? k?t n?i port 9000. `n Backup Server: ch? nh?n k?t n?i t? Database Server và Application Server. `n 5.7 Audit & Nh?t ký b?o m?t `n S? ki?n `n Ghi vào `n Thông tin luu `n Ðang nh?p thành công `n system_log `n userId, IP, th?i gian, user-agent `n Ðang nh?p th?t b?i `n system_log `n username, IP, th?i gian, lý do `n Ðang xu?t `n system_log `n userId, IP, th?i gian `n T?o/s?a/xóa b?n ghi `n system_log `n userId, donViId, action, entityType, entityId, before/after JSON `n Reset m?t kh?u `n system_log `n adminId, targetUserId, IP `n Thay d?i phân quy?n `n system_log `n adminId, targetUserId, role changes `n Export d? li?u `n system_log `n userId, module, filter params, s? records `n Upload/Delete file `n system_log `n userId, fileName, fileSize, entityType `n 5.8 K? ho?ch ?ng phó s? c? `n Tình hu?ng `n Hành d?ng `n Th?i gian ph?n ?ng `n Phát hi?n tài kho?n b? xâm ph?m `n Lock account, revoke refresh token, review log 7 ngày g?n nh?t `n < 1 gi? `n L? thông tin nh?y c?m `n Ðánh giá ph?m vi, thông báo H05, vá l? h?ng, rotate secrets `n < 4 gi? `n T?n công brute force `n Auto-lock (5 l?n sai), blacklist IP, thông báo H05_ADMIN `n T? d?ng `n Server b? t?n công `n Cô l?p server, khôi ph?c t? backup s?ch nh?t, forensics `n < RTO 4h `n Token b? dánh c?p `n Rotate signing key ? invalidate m?i token hi?n t?i ? force re-login t?t c? `n < 30 phút `n 6. Thi?t k? Tri?n khai & V?n hành (Deployment Design) `n Ph?n này mô t? c?u hình môi tru?ng, containerization, CI/CD và chi?n lu?c backup/monitoring. `n 6.1 Môi tru?ng tri?n khai `n Môi tru?ng `n M?c dích `n H? t?ng `n URL `n Development `n Dev local — t?ng developer `n Laptop cá nhân, Docker Desktop `n localhost:4200 / :5000 `n Staging `n Test tích h?p, UAT n?i b? H05 `n VM H05 staging — cùng c?u hình prod `n staging.thuc-luc.h05.local `n Production `n H? th?ng th?t cho 100+ don v? `n Server H05 BCANet `n thuc-luc.h05.local `n 6.2 C?u hình Server Production `n Server `n Vai trò `n C?u hình t?i thi?u khuy?n ngh? `n OS `n Application Server `n Nginx + .NET runtime + Angular static `n 8 vCPU, 16 GB RAM, 100 GB SSD `n RHEL 8 / Ubuntu 22.04 `n Database Server `n Oracle Database 19c+ `n 8 vCPU, 32 GB RAM, 500 GB SSD `n RHEL 8 (Oracle yêu c?u) `n MinIO Server `n Object Storage S3-compatible `n 4 vCPU, 8 GB RAM, 2 TB HDD `n RHEL 8 / Ubuntu 22.04 `n Backup Server `n Full + incremental backup storage `n 4 vCPU, 8 GB RAM, 4 TB HDD `n RHEL 8 / Ubuntu 22.04 `n 6.3 Docker & Container `n 6.3.1 Docker Compose (Development & Staging) `n volumes: [minio_data:/data] gotenberg: image: gotenberg/gotenberg:8 ports: ["3000:3000"] command: - gotenberg - "--chromium-disable-javascript=true" `n 6.3.2 Dockerfile Backend (.NET) `n FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build WORKDIR /src COPY *.csproj . && dotnet restore COPY . . && dotnet publish -c Release -o /app FROM mcr.microsoft.com/dotnet/aspnet:8.0 WORKDIR /app COPY --from=build /app . ENV ASPNETCORE_URLS=http://+:5000 ENTRYPOINT ["dotnet", "ThucLucApi.dll"] `n 6.3.3 Dockerfile Frontend (Angular + Nginx) `n FROM node:20-alpine AS build WORKDIR /app COPY package*.json . && npm ci COPY . . && npm run build -- --configuration=production FROM nginx:1.25-alpine COPY --from=build /app/dist/thuc-luc-v2/browser /usr/share/nginx/html COPY nginx.conf /etc/nginx/conf.d/default.conf EXPOSE 80 `n 6.4 Nginx Configuration `n server { listen 443 ssl; server_name thuc-luc.h05.local; ssl_certificate /etc/ssl/h05/cert.pem; ssl_certificate_key /etc/ssl/h05/key.pem; ssl_protocols TLSv1.2 TLSv1.3; # Angular SPA — m?i route tr? v? index.html root /usr/share/nginx/html; index index.html; location / { try_files $uri $uri/ /index.html; } # Proxy API ? Backend ASP.NET Core location /api/ { proxy_pass http://backend:5000; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_read_timeout 60s; } # Gzip gzip on; gzip_types text/plain application/javascript application/json text/css; gzip_min_length 1000; } `n 6.5 CI/CD Pipeline `n Pipeline ch?y trên GitLab CI/CD (self-hosted t?i H05 — không Internet): `n Stage `n Jobs `n Trigger `n build `n dotnet build, ng build --prod `n Push to any branch `n test `n dotnet test, ng test --watch=false `n Push to any branch `n lint `n dotnet format --verify, eslint --max-warnings=0 `n Push to any branch `n staging `n docker build + push registry, deploy docker-compose `n Merge to develop `n prod `n docker pull + rolling deploy, smoke test `n Merge to main (manual trigger) `n Secrets (ORACLE_CONN, JWT keys, MinIO keys) luu trong GitLab CI Variables — không commit vào repo. `n Production deploy: rolling update — stop old container sau khi new container healthy. `n Rollback: re-deploy image tag c?a version tru?c (image không xóa trong 30 ngày). `n 6.6 Backup & Recovery `n 6.6.1 Chi?n lu?c Backup `n Lo?i backup `n T?n su?t `n Công c? `n Retention `n M?c tiêu `n Oracle Full Backup `n Hàng tu?n (Ch? nh?t 02:00) `n RMAN `n 4 tu?n `n RPO tu?n `n Oracle Incremental `n Hàng ngày (02:00 — tr? CN) `n RMAN `n 7 ngày `n RPO 24h `n Oracle Archive Log `n M?i 4 gi? `n RMAN `n 7 ngày `n RPO 4h `n MinIO snapshot `n Hàng ngày (03:00) `n mc mirror `n 30 ngày `n File attachment `n Config & secrets `n Khi có thay d?i `n Git + manual `n Vinh vi?n `n C?u hình h? th?ng `n 6.6.2 Recovery Objectives `n Ch? s? `n M?c tiêu `n K?ch b?n `n RPO (Recovery Point Objective) `n <= 24 gi? `n M?t d? li?u t?i da 1 ngày làm vi?c `n RTO (Recovery Time Objective) `n <= 4 gi? `n Khôi ph?c h? th?ng trong 4 gi? `n MTTR (Mean Time to Recovery) `n <= 2 gi? `n Tru?ng h?p l?i thông thu?ng `n Backup verification `n Hàng tháng `n Test restore vào môi tru?ng staging `n 6.7 Monitoring & Alerting `n Metric `n Công c? `n Ngu?ng c?nh báo `n CPU / Memory / Disk `n Prometheus + Node Exporter `n CPU > 80%, RAM > 85%, Disk > 90% `n HTTP error rate `n Prometheus + Nginx metrics `n 5xx rate > 1% trong 5 phút `n API response time `n Prometheus + ASP.NET metrics `n P95 > 2 giây `n Oracle connections `n Oracle Enterprise Manager `n > 80% max connections `n MinIO storage `n MinIO Console metrics `n Disk > 80% `n Backup job `n Cron + script g?i alert `n Job th?t b?i ho?c file backup không tang `n Login failures `n Query system_log `n > 20 l?n th?t b?i/IP trong 5 phút `n C?nh báo g?i qua: email n?i b? H05 (SMTP n?i m?ng) + popup trong giao di?n admin. `n Dashboard Grafana (self-hosted) k?t n?i Prometheus — hi?n th? metric real-time. `n 7. Chi?n lu?c Ki?m th? (Testing Strategy) `n Ph?n này mô t? các m?c ki?m th?, công c?, ph?m vi và tiêu chí pass/fail cho h? th?ng ThucLuc V2. `n 7.1 T?ng quan & Ph?m vi ki?m th? `n M?c ki?m th? `n Công c? `n Ph?m vi `n Ngu?i th?c hi?n `n Unit Test `n xUnit (.NET), Jasmine/Jest (Angular) `n Services, Validators, Pipes, Pure functions `n Developer `n Integration Test `n xUnit + WebApplicationFactory `n API endpoints v?i DB th?t (Oracle test schema) `n Developer `n E2E Test `n Cypress `n Lu?ng nghi?p v? chính (smoke test) `n QA / Developer `n Performance Test `n k6 `n API endpoints quan tr?ng du?i t?i cao `n Developer / DevOps `n Security Test `n OWASP ZAP, manual review `n OWASP Top 10, authentication flows `n Security reviewer `n UAT `n Manual — k?ch b?n t? SRS `n Toàn b? tính nang ngu?i dùng cu?i `n H05 + don v? pilot `n 7.2 Unit Testing `n 7.2.1 Backend (.NET — xUnit) `n Module test `n N?i dung ki?m tra `n Coverage m?c tiêu `n Services `n Business logic, validation rule BR-*, edge cases `n >= 80% `n Validators `n FluentValidation rules — h?p l? + không h?p l? `n 100% `n Authorization `n RequirePermission attribute v?i các role khác nhau `n 100% `n Repository (mock) `n Query filter, soft-delete, pagination `n >= 70% `n JWT Helper `n Generate / validate / expire token `n 100% `n // Ví d? xUnit test [Fact] public async Task NopBaoCao_WhenKyLocked_ShouldThrow422() { var ky = new KyBaoCao { TrangThai = TrangThaiKy.KHOA }; var svc = new NhanLucCnttService(_repoMock, _kyRepoMock); var ex = await Assert.ThrowsAsync<BusinessException>( () => svc.NopAsync(recordId: 1, userId: 1)); Assert.Equal("KY_LOCKED", ex.Code); } `n 7.2.2 Frontend (Angular — Jest) `n Component/Service `n N?i dung ki?m tra `n AuthService `n Login flow, token storage, logout, refresh `n HasPermissionDirective `n ?n/hi?n element dúng theo permissions[] `n DataTableComponent `n Render dúng d? li?u, phân trang, sort `n Pipes (DateVN, FileSize) `n Transform output dúng v?i các input edge case `n NghiepVuService `n HTTP calls dúng endpoint, handle error 4xx/5xx `n 7.3 Integration Testing `n 7.3.1 API Integration Test `n S? d?ng WebApplicationFactory<Program> — test server th?t v?i Oracle schema riêng cho test: `n // Test fixture public class ApiTestFixture : WebApplicationFactory<Program> { // Override Oracle connection ? test schema: CAND_TEST // Seed d? li?u tru?c m?i test run // Rollback transaction sau m?i test } [Fact] public async Task PostNhanLuc_ValidData_Returns201() { var client = _fixture.CreateClientWithToken(role: "DON_VI_USER"); var response = await client.PostAsJsonAsync("/api/v1/nhan-luc-cntt", validDto); Assert.Equal(HttpStatusCode.Created, response.StatusCode); } `n 7.3.2 K?ch b?n Integration Test quan tr?ng `n Test case `n Mô t? `n Expected `n TC-INT-01 `n Login dúng credentials `n 200 + accessToken `n TC-INT-02 `n Login sai password 5 l?n `n 401, l?n 5 ? tài kho?n b? lock `n TC-INT-03 `n DON_VI_USER t?o b?n ghi k? dang MO `n 201 `n TC-INT-04 `n DON_VI_USER t?o b?n ghi k? dã KHOA `n 422 KY_LOCKED `n TC-INT-05 `n DON_VI_USER truy c?p d? li?u don v? khác `n 403 `n TC-INT-06 `n Upload file > 25 MB `n 400 FILE_TOO_LARGE `n TC-INT-07 `n Upload file .exe `n 400 FILE_TYPE_NOT_ALLOWED `n TC-INT-08 `n H05_ADMIN xem t?ng h?p t?t c? don v? `n 200 + d? d? li?u `n TC-INT-09 `n N?p báo cáo ? GET tr?ng thái = DA_NOP `n 200 + TrangThai=DA_NOP `n TC-INT-10 `n Phê duy?t ? GET tr?ng thái = DA_DUYET `n 200 + TrangThai=DA_DUYET `n TC-INT-11 `n Truy c?p không có token `n 401 `n TC-INT-12 `n Truy c?p v?i token h?t h?n `n 401 TOKEN_EXPIRED `n TC-INT-13 `n Audit log du?c ghi khi POST/PUT/DELETE `n system_log có record `n 7.4 E2E Testing (Cypress) `n 7.4.1 Lu?ng E2E chính `n Test ID `n Lu?ng ki?m th? `n Steps `n E2E-01 `n Ðang nh?p ? Dashboard `n M? /login ? nh?p credentials ? verify redirect dashboard ? verify widget hi?n th? `n E2E-02 `n Nh?p li?u module nghi?p v? hoàn ch?nh `n Ch?n k? ? vào module ? t?o b?n ghi ? luu nháp ? n?p ? verify tr?ng thái DA_NOP `n E2E-03 `n Phê duy?t báo cáo (H05_ADMIN) `n Login H05_ADMIN ? vào phê duy?t ? duy?t ? verify DA_DUYET trong list `n E2E-04 `n Yêu c?u b? sung ? don v? ch?nh s?a `n H05_ADMIN yêu c?u b? sung ? DON_VI_USER nh?n thông báo ? ch?nh s?a ? n?p l?i `n E2E-05 `n Xu?t Excel báo cáo `n Login H05_ADMIN ? Báo cáo t?ng h?p ? Xu?t Excel ? verify file download `n E2E-06 `n Qu?n lý k? báo cáo `n T?o k? m?i ? m? k? ? verify don v? th?y k? ? dóng k? ? verify không nh?p du?c `n 7.5 Performance Testing (k6) `n M?c tiêu hi?u nang h? th?ng theo SRS NFR-PERF: `n K?ch b?n `n Load `n M?c tiêu `n Login concurrent `n 50 users d?ng th?i `n P95 < 1s, 0% error `n Danh sách module (GET list) `n 100 concurrent users `n P95 < 2s, 0% error `n T?o b?n ghi (POST) `n 50 concurrent users `n P95 < 1s, 0% error `n Export Excel báo cáo `n 10 concurrent users `n P95 < 10s, 0% error `n Dashboard widgets (5 API) `n 100 concurrent users `n P95 < 3s, 0% error `n Soak test — 4 gi? liên t?c `n 30 concurrent users `n Không memory leak, P95 ?n d?nh `n // k6 script m?u import http from 'k6/http'; import { check, sleep } from 'k6'; export const options = { stages: [ { duration: '2m', target: 50 }, // ramp up { duration: '5m', target: 50 }, // steady { duration: '1m', target: 0 }, // ramp down ], thresholds: { http_req_duration: ['p(95)<2000'], http_req_failed: ['rate<0.01'], }, }; `n 7.6 Security Testing `n Test `n Công c? / Phuong pháp `n K?ch b?n `n Broken Access Control `n Manual + Postman `n G?i API don v? khác b?ng token don v? mình `n SQL Injection `n OWASP ZAP scanner `n Inject vào query params, body fields `n XSS `n OWASP ZAP + Manual `n Script injection qua form fields `n Brute force login `n Burp Suite `n Th? 100 password liên ti?p ? verify rate limit `n JWT manipulation `n jwt.io + Manual `n S?a payload claims, dùng none algorithm `n Privilege escalation `n Manual `n DON_VI_USER g?i system:admin endpoint `n File upload bypass `n Burp Suite `n Ð?i Content-Type d? upload .exe, .php `n Sensitive data in log `n Review log files `n Ki?m tra không có token/password trong log `n 7.7 User Acceptance Testing (UAT) `n 7.7.1 K? ho?ch UAT `n Giai do?n `n N?i dung `n Ngu?i th?c hi?n `n Th?i gian `n UAT-1 Alpha `n Test n?i b? H05 — team phát tri?n + nghi?p v? `n H05 n?i b? `n 2 tu?n `n UAT-2 Pilot `n Tri?n khai th? 3-5 don v? d?i di?n `n Ðon v? pilot + H05 `n 4 tu?n `n UAT-3 Full `n Tri?n khai toàn b? 100+ don v? `n T?t c? don v? CAND `n 1 k? báo cáo `n 7.7.2 Tiêu chí ch?p nh?n (Pass criteria) `n T?t c? TC-INT-01 d?n TC-INT-13 pass 100%. `n T?t c? E2E-01 d?n E2E-06 pass 100%. `n Performance: P95 response time < 2s v?i 100 concurrent users. `n Zero critical/high security finding t? OWASP ZAP scan. `n UAT Pilot: không có bug severity Critical/High nào còn m? sau 2 tu?n. `n Tài li?u hu?ng d?n s? d?ng (User Manual) du?c H05 nghi?m thu. `n 7.7.3 Ð?nh nghia Bug Severity `n Severity `n Ð?nh nghia `n SLA fix `n Critical `n M?t d? li?u, không dang nh?p du?c, l? h?ng b?o m?t nghiêm tr?ng `n < 4 gi? `n High `n Ch?c nang nghi?p v? chính không ho?t d?ng, không xu?t du?c báo cáo `n < 1 ngày `n Medium `n Ch?c nang ph? l?i, hi?n th? sai nhung workflow v?n ho?t d?ng `n < 3 ngày `n Low `n L?i giao di?n nh?, typo, tr?i nghi?m ngu?i dùng chua t?t `n Backlog `n 8. Ph? l?c `n 8.1 Thu?t ng? & Vi?t t?t `n Thu?t ng? `n Gi?i thích `n BCANet `n M?ng n?i b? c?a B? Công An `n H05 `n C?c Công ngh? thông tin — B? Công An `n CAND `n Công an nhân dân `n SDD `n System Design Document — Tài li?u thi?t k? h? th?ng `n SRS `n Software Requirements Specification — Ð?c t? yêu c?u ph?n m?m `n RBAC `n Role-Based Access Control — Ki?m soát truy c?p theo vai trò `n JWT `n JSON Web Token — Chu?n token xác th?c `n TDE `n Transparent Data Encryption — Mã hóa d? li?u Oracle `n RMAN `n Recovery Manager — Công c? backup/restore Oracle `n RPO `n Recovery Point Objective — Ði?m ph?c h?i m?c tiêu (th?i gian m?t d? li?u t?i da) `n RTO `n Recovery Time Objective — Th?i gian ph?c h?i m?c tiêu `n EF Core `n Entity Framework Core — ORM .NET `n ODP.NET `n Oracle Data Provider for .NET — Driver k?t n?i Oracle `n MinIO `n Object Storage S3-compatible, self-hosted `n NgRx `n NgRx — Angular State Management (Redux pattern) `n UAT `n User Acceptance Testing — Ki?m th? nghi?m thu `n CI/CD `n Continuous Integration / Continuous Deployment `n SPA `n Single Page Application — ?ng d?ng web m?t trang `n 8.2 Tài li?u tham chi?u `n Tài li?u `n Phiên b?n `n Mô t? `n Requirement_Specification.docx `n v0.2 `n Ð?c t? yêu c?u ph?n m?m ThucLuc V2 `n ASP.NET Core 8 Documentation `n 8.0 `n docs.microsoft.com/aspnet/core `n Angular Documentation `n 17.x `n angular.dev `n Oracle EF Core Provider `n 8.x `n Oracle.EntityFrameworkCore NuGet `n MinIO .NET SDK `n latest `n minio.github.io/minio-dotnet `n OWASP Top 10 `n 2021 `n owasp.org/Top10 `n JWT RFC 7519 `n - `n tools.ietf.org/html/rfc7519 `n k6 Documentation `n latest `n k6.io/docs `n --- H?t tài li?u System Design Document v0.8 --- `n 
