@page "/error"

<PageTitle>Lỗi · Trung tâm vận hành</PageTitle>

<section class="empty-state empty-state--error">
    <span class="empty-state__icon">!</span>
    <strong>Ops Console gặp lỗi</strong>
    <p>Kiểm tra log của service trước khi thử lại. Không có thao tác DB nào được tự động tiếp tục.</p>
    <a class="button button--primary" href="/">Về tổng quan</a>
</section>
